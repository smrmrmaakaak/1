const SKILLS = {
  "kage-landing-page": "---\nname: add-kage-landing-page\ndescription: \"Build Kage from its verified authored source using Full HTML + DOM/CSS + Three.js, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Kage\n\n## Description\n\nThe complete authored Kage temple experience, preserved as an interactive full-page document with its original navigation, scroll scenes, and local Three.js world.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React iframe host\n- Byte-exact complete authored HTML document\n- Same-project local source URL\n- Local Three.js runtime, embedded font stylesheet, fourteen WebP scene layers, and eight MP3 tracks\n\n## Verified source material\n\n- `src/shaders/landing-pages/LandingPages.tsx`\n- `public/landing-pages/kage.html — complete page with audio removed`\n- `public/landing-pages/secret-pathways-assets/fonts.css`\n- `public/landing-pages/secret-pathways-assets/three.min.js`\n\nSource revision: `SHA-256 c8e06b90397a`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Copy the complete Kage HTML file byte-for-byte to /landing-pages/kage.html; do not extract, rewrite, shorten, or rebrand any section.\n3. Preserve every embedded style, script, media payload, text string, interaction, responsive rule, and document-level lifecycle.\n4. Keep every relative local asset at the exact path expected by the original document.\n5. Load the local document in a full-size iframe whose permissions retain the authored forms, modals, downloads, popups, scripts, and same-origin resources.\n6. Lazy-load only the React host bundle; do not import the complete HTML into the application JavaScript graph.\n7. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: Copy kage.html plus the original secret-pathways-assets stylesheet, Three.js runtime, fourteen WebP scene layers, and eight MP3 tracks without changing their relative paths.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { KageLandingPage } from \"./effects/kage-landing-page/KageLandingPage\";\nimport \"./effects/kage-landing-page/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><KageLandingPage /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<LandingPageFrame title=\"Kage\" sourceUrl=\"/landing-pages/kage.html\" />\n```\n\n## Behavior contract\n\n- Runtime: Full HTML + DOM/CSS + Three.js\n- Passes: 1 sandboxed full-document renderer\n- Interaction: Original pointer, keyboard, scroll, and navigation interactions\n- Assets: Fourteen local WebP scene layers\n- **document** (fixed): Complete packaged kage.html with audio removed\n- **sourceUrl** (fixed): /landing-pages/kage.html\n- **headingFont** (optional): Onest | Instrument Serif | Newsreader | Geist\n- **bodyFont** (optional): Onest | Geist | Newsreader | Instrument Serif\n- **headingWeight** (optional): 400 | 500 | 600 | 700\n- **bodyWeight** (optional): 300 | 400 | 500 | 600\n- **primaryColor** (optional): Hex color — the vermilion accent and the ember tint it drives\n- **typography** (optional): Heading size 30–72px ceiling + body size + heading tracking\n- **layout** (responsive): Original full landing page inside the preview frame\n- **interaction** (original): Scroll + pointer + keyboard\n- **assets** (fixed): 14 local binary assets + local fonts and Three.js runtime\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "complete-shelf-landing-page": "---\nname: add-complete-shelf-landing-page\ndescription: \"Build Complete Shelf from its verified authored source using Full HTML + DOM/CSS + Three.js r165, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Complete Shelf\n\n## Description\n\nThe complete Working Volumes bookshelf page, preserved unchanged with all seven tools, its responsive editorial interface, and authored Three.js presentation.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React iframe host\n- Byte-exact complete authored HTML document\n- Same-project local source URL\n- Three.js r165 and all original embedded media\n\n## Verified source material\n\n- `src/shaders/landing-pages/LandingPages.tsx`\n- `public/landing-pages/complete-shelf-v2.html — byte-exact complete page`\n\nSource revision: `SHA-256 53fe658f794d`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Copy the complete Complete Shelf HTML file byte-for-byte to /landing-pages/complete-shelf-v2.html; do not extract, rewrite, shorten, or rebrand any section.\n3. Preserve every embedded style, script, media payload, text string, interaction, responsive rule, and document-level lifecycle.\n4. Keep every relative local asset at the exact path expected by the original document.\n5. Load the local document in a full-size iframe whose permissions retain the authored forms, modals, downloads, popups, scripts, and same-origin resources.\n6. Lazy-load only the React host bundle; do not import the complete HTML into the application JavaScript graph.\n7. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: Copy complete-shelf-v2.html byte-for-byte; its authored media remains embedded in the document.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { CompleteShelfLandingPage } from \"./effects/complete-shelf-landing-page/CompleteShelfLandingPage\";\nimport \"./effects/complete-shelf-landing-page/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><CompleteShelfLandingPage /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<LandingPageFrame title=\"Complete Shelf\" sourceUrl=\"/landing-pages/complete-shelf-v2.html\" />\n```\n\n## Behavior contract\n\n- Runtime: Full HTML + DOM/CSS + Three.js r165\n- Passes: 1 sandboxed full-document renderer\n- Interaction: Original pointer, keyboard, scroll, navigation, and book interactions\n- Assets: All visual and media data remains embedded in the original HTML\n- **document** (fixed): Complete original complete-shelf-v2.html, byte-for-byte\n- **sourceUrl** (fixed): /landing-pages/complete-shelf-v2.html\n- **headingFont** (optional): Iowan Old Style | Instrument Serif | Newsreader | Geist\n- **bodyFont** (optional): Inter | Geist | Newsreader | Instrument Serif\n- **headingWeight** (optional): 400 | 500 | 600\n- **bodyWeight** (optional): 400 | 500 | 600\n- **primaryColor** (optional): Hex color\n- **typography** (optional): Heading size + body size + heading letter spacing\n- **layout** (responsive): Original full landing page inside the preview frame\n- **interaction** (original): Scroll + pointer + keyboard + book controls\n- **assets** (embedded): Original embedded media\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "bestsellers-book-showcase": "---\nname: add-bestsellers-book-showcase\ndescription: \"Build Bestsellers Book Showcase from its verified authored source using Full HTML + DOM/CSS + embedded media, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Bestsellers Book Showcase\n\n## Description\n\nThe complete Field Manuals book showcase, preserved unchanged with its editorial layout, authored motion, interactions, and embedded media.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React iframe host\n- Byte-exact complete authored HTML document\n- Same-project local source URL\n- All original media embedded in the authored HTML\n\n## Verified source material\n\n- `src/shaders/landing-pages/LandingPages.tsx`\n- `public/landing-pages/bestsellers-book-showcase.html — byte-exact complete page`\n\nSource revision: `SHA-256 7c1ed1ca4a4c`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Copy the complete Bestsellers Book Showcase HTML file byte-for-byte to /landing-pages/bestsellers-book-showcase.html; do not extract, rewrite, shorten, or rebrand any section.\n3. Preserve every embedded style, script, media payload, text string, interaction, responsive rule, and document-level lifecycle.\n4. Keep every relative local asset at the exact path expected by the original document.\n5. Load the local document in a full-size iframe whose permissions retain the authored forms, modals, downloads, popups, scripts, and same-origin resources.\n6. Lazy-load only the React host bundle; do not import the complete HTML into the application JavaScript graph.\n7. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: Copy bestsellers-book-showcase.html byte-for-byte; its authored media remains embedded in the document.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { BestsellersBookShowcase } from \"./effects/bestsellers-book-showcase/BestsellersBookShowcase\";\nimport \"./effects/bestsellers-book-showcase/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><BestsellersBookShowcase /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<LandingPageFrame title=\"Bestsellers Book Showcase\" sourceUrl=\"/landing-pages/bestsellers-book-showcase.html\" />\n```\n\n## Behavior contract\n\n- Runtime: Full HTML + DOM/CSS + embedded media\n- Passes: 1 sandboxed full-document renderer\n- Interaction: Original pointer, keyboard, scroll, navigation, and showcase interactions\n- Assets: All visual and video data remains embedded in the original HTML\n- **document** (fixed): Complete original bestsellers-book-showcase.html, byte-for-byte\n- **sourceUrl** (fixed): /landing-pages/bestsellers-book-showcase.html\n- **headingFont** (optional): Iowan Old Style | Instrument Serif | Newsreader | Geist\n- **bodyFont** (optional): Iowan Old Style | Geist | Newsreader | Instrument Serif\n- **headingWeight** (optional): 400 | 500 | 600 | 700\n- **bodyWeight** (optional): 400 | 500 | 600 | 700\n- **primaryColor** (optional): Hex color\n- **typography** (optional): Heading size + body size + heading letter spacing\n- **layout** (responsive): Original full landing page inside the preview frame\n- **interaction** (original): Scroll + pointer + keyboard + media controls\n- **assets** (embedded): Original embedded media\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "sylva-hero": "---\nname: add-sylva-hero\ndescription: \"Build Sylva from its verified Community source with the complete renderer, interactions, controls, and required assets.\"\n---\n\n# Build Sylva\n\nThe complete Sylva Living Green page, preserved with its Three.js scene, local typography, card imagery, and embedded liquid-metal buttons. The authored page exactly as written: moss-root world, pale flowers, ferns, drifting pollen, and the landing butterfly behind the full hero layout.\n\n## Verified source material\n\n- `src/shaders/landing-pages/LandingPages.tsx`\n- `public/landing-pages/inner-green-3d.html — byte-exact complete page`\n\nSource revision: `SHA-256 fd922291297d`\n\n## Requirements\n\n- Preserve the complete Full HTML + DOM/CSS + local Three.js renderer and every published Community option.\n- Preserve its interaction contract: Original pointer, hover, scroll, and responsive scaling.\n- Preserve its assets: Six local assets are packaged at their authored relative paths; the page makes no external request.\n- Verify resize, mobile, reduced motion, visibility, teardown, and console health.\n",
  "meng-to-sketchbook-landing-page": "---\nname: add-meng-to-sketchbook-landing-page\ndescription: \"Build Sketchbook from its verified authored source using Full HTML + DOM/CSS + JavaScript, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Sketchbook\n\n## Description\n\nA tactile personal portfolio built as a Singapore sketchbook, with nine illustrated plates, curled page turns, a draggable magnifying glass, zoom controls, a botanical paper atmosphere, and an editorial index.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React iframe host\n- Byte-exact complete authored HTML document\n- Same-project local source URL\n- Fourteen local paper, botanical, and Singapore illustration images plus three local Instrument Serif and Newsreader font files\n\n## Verified source material\n\n- `src/shaders/landing-pages/LandingPages.tsx`\n- `public/landing-pages/meng-to-sketchbook.html — byte-exact complete page`\n- `public/landing-pages/meng-to-sketchbook/ — 17 local image and font assets`\n\nSource revision: `SHA-256 e0330548b1ac`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Copy the complete Meng To Sketchbook HTML file byte-for-byte to /landing-pages/meng-to-sketchbook.html; do not extract, rewrite, shorten, or rebrand any section.\n3. Preserve every embedded style, script, media payload, text string, interaction, responsive rule, and document-level lifecycle.\n4. Keep every relative local asset at the exact path expected by the original document.\n5. Load the local document in a full-size iframe whose permissions retain the authored forms, modals, downloads, popups, scripts, and same-origin resources.\n6. Lazy-load only the React host bundle; do not import the complete HTML into the application JavaScript graph.\n7. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: Copy meng-to-sketchbook.html byte-for-byte and keep all 17 files under meng-to-sketchbook/ at exactly that relative path. The page needs no network access.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { MengToSketchbookLandingPage } from \"./effects/meng-to-sketchbook-landing-page/MengToSketchbookLandingPage\";\nimport \"./effects/meng-to-sketchbook-landing-page/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><MengToSketchbookLandingPage /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<LandingPageFrame title=\"Meng To Sketchbook\" sourceUrl=\"/landing-pages/meng-to-sketchbook.html\" />\n```\n\n## Behavior contract\n\n- Runtime: Full HTML + DOM/CSS + JavaScript\n- Passes: 1 sandboxed full-document renderer\n- Interaction: Original page turns, pointer tilt, draggable magnifier, zoom controls, navigation, scroll, keyboard, and responsive layout\n- Assets: Fourteen local paper, botanical, and Singapore illustration images plus three local variable and display fonts\n- **document** (fixed): Complete original meng-to-sketchbook.html, byte-for-byte\n- **sourceUrl** (fixed): /landing-pages/meng-to-sketchbook.html\n- **layout** (responsive): Original full landing page inside the preview frame\n- **interaction** (original): Page turns + magnifier drag + zoom + pointer tilt + scroll + keyboard\n- **assets** (local): 17 packaged files in public/landing-pages/meng-to-sketchbook/; no network request\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "predictive-arc": "---\nname: add-predictive-arc\ndescription: \"Build Predictive Arc from its verified authored source using Canvas 2D + Raw WebGL + Three.js r128, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Predictive Arc\n\n## Description\n\nEight animated arc, signal, ribbon, void, and halftone scenes collected in one Canvas 2D, raw-WebGL, and Three.js family.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React variant host\n- Four Canvas 2D renderers\n- Three raw-WebGL renderers\n- One Three.js point-field renderer\n- Dark/light mode surfaces\n- Lazy-loaded variant sources\n- ResizeObserver, IntersectionObserver, adaptive pixel ratio, and requestAnimationFrame\n\n## Verified source material\n\n- `Axiom---Predictive-Search-Engine (3).html — predictive arc source`\n- `Axiom-Dynamic-Data-Orchestration.html — data pixel arc source`\n- `src/shaders/predictive-arc/predictiveArcRenderer.ts`\n- `src/shaders/data-pixel-arc/dataPixelArcRenderer.ts`\n- `src/shaders/neuform-isolated/sources/signal-particles.html`\n- `src/shaders/neuform-isolated/sources/override-grid.html`\n- `src/shaders/neuform-isolated/NeuformBatchEffects.tsx`\n- `src/shaders/ribbon-field/RibbonFieldBackground.tsx`\n- `src/shaders/ribbon-field/ribbonFieldShaders.ts`\n- `src/shaders/neuform-isolated/NeuformIsolatedEffects.tsx`\n- `src/shaders/neuform-isolated/sources/void-protocol.html`\n- `src/shaders/neuform-isolated/NeuformCraftEffects.tsx`\n- `src/shaders/neuform-isolated/sources/nexus-unified-flow.html`\n- `src/shaders/neuform-isolated/sources/amber-halftone.html`\n- `src/shaders/predictive-arc/PredictiveArcCollection.tsx`\n- `src/shaders/predictive-arc/PredictiveArcCanvas.tsx`\n\nSource revision: `SHA-256 fa86582fc870`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Keep Predictive Arc as the public entry point and select predictive, data-pixel, signal-particles, override-grid, ribbon-field, void-field, halftone-flow, or amber-halftone with the variant prop.\n3. Retain each authored renderer and its original composition instead of blending the scenes into one canvas.\n4. Expose the shared mode, speed, hue, saturation, and brightness controls at the collection boundary.\n5. Lazy-load the isolated Signal Particles, Override Grid, Ribbon Field, Void Field, Halftone Flow, and Amber Halftone renderers so only the selected variant runs.\n6. Preserve each renderer's resize, visibility, animation-frame, and cleanup lifecycle.\n7. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { PredictiveArcCanvas } from \"./effects/predictive-arc/PredictiveArcCanvas\";\nimport \"./effects/predictive-arc/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><PredictiveArcCanvas /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<PredictiveArcCanvas variant=\"ribbon-field\" speed={1.2} hue={12} />\n```\n\n## Behavior contract\n\n- Runtime: Canvas 2D + Raw WebGL + Three.js r128\n- Passes: 1 selected Canvas 2D, raw-WebGL, or Three.js field pass\n- Interaction: Variant selection plus customizable mode, speed, color, and brightness\n- Assets: No external assets\n- **renderer** (host): Canvas 2D + Raw WebGL + Three.js\n- **variants** (fixed): Predictive + Data Pixel + Signal + Override + Ribbon + Void + Halftone Flow + Amber Halftone\n- **mode** (optional): dark | light\n- **pixelRatio** (adaptive): ≤ 2\n- **assets** (fixed): None\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "liquid-form": "---\nname: add-liquid-form\ndescription: \"Build Liquid Form from its verified authored source using Raw WebGL, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Liquid Form\n\n## Description\n\nA centered silver ray-marched liquid form with authored studio reflections and pointer-responsive camera drift.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React lifecycle host\n- Raw WebGL 1 fullscreen quad\n- High-precision GLSL simplex noise and 70-step signed-distance-field ray march\n- Smoothed pointer look-at and adaptive 1.5x DPR\n\n## Verified source material\n\n- `V-E-L-O-X-Quantum-Engineered.html — WebGL background`\n- `src/shaders/liquid-form/liquidFormShaders.ts`\n- `src/shaders/liquid-form/LiquidFormBackground.tsx`\n\nSource revision: `SHA-256 acc0cbacb914`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Compile the exact full-screen vertex shader and complete silver liquid metal fragment shader.\n3. Keep all three noise octaves, SDF normals, environment lights, Fresnel, specular, bloom, tone mapping, and gamma stages.\n4. Map geometric customization to uniforms, then optionally tint the final silver canvas; tint amount zero must remain source-exact.\n5. Preserve pointer smoothing, resize, visibility pausing, context ownership, and complete WebGL resource cleanup.\n6. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { LiquidFormBackground } from \"./effects/liquid-form/LiquidFormBackground\";\nimport \"./effects/liquid-form/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><LiquidFormBackground /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\ngl.uniform1f(uniforms.morph, options.morph);\ngl.uniform1f(uniforms.noiseScale, options.noiseScale);\ngl.drawArrays(gl.TRIANGLE_STRIP, 0, 4);\n```\n\n## Behavior contract\n\n- Runtime: Raw WebGL\n- Passes: 1 raw WebGL ray-march pass\n- Interaction: Smoothed pointer look-at plus customizable material, morph, and tint\n- Assets: No external assets\n- **renderer** (host): Raw WebGL\n- **steps** (fixed): 70\n- **pixelRatio** (adaptive): ≤ 1.5\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "crt": "---\nname: add-crt\ndescription: \"Build CRT from its verified authored source using Raw WebGL + Canvas 2D, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build CRT\n\n## Description\n\nA complete Matrix-era boot terminal rendered to an offscreen text texture and passed through the exact authored curved CRT shader.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React lifecycle host\n- Raw WebGL CRT composite\n- Offscreen Canvas 2D boot-text texture\n- Visibility-aware animation and adaptive backing resolution\n\n## Verified source material\n\n- `ZION-Construct-Initialization (1).html — complete CRT background`\n- `src/shaders/crt/crtRenderer.ts`\n- `src/shaders/crt/crtShaders.ts`\n- `src/shaders/crt/CrtBackground.tsx`\n\nSource revision: `SHA-256 860a1eb1d4c9`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Keep the complete 19-row terminal log, colors, layout math, cursor, reveal cadence, and blink state.\n3. Upload the offscreen Canvas texture only when the boot text changes.\n4. Compile the exact authored curvature, chromatic offset, scanline, grille, rolling bar, vignette, flicker, and grain GLSL.\n5. Apply brightness, opacity, and a hue/saturation grade to the completed host rather than editing the texture or GLSL.\n6. Own texture, buffer, shader, observer, resize, frame, and disposal lifecycle.\n7. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { CrtBackground } from \"./effects/crt/CrtBackground\";\nimport \"./effects/crt/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><CrtBackground /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\ndrawScreen(reveal);\nif (cursorVisible) drawCursor();\ngl.texImage2D(gl.TEXTURE_2D, 0, gl.RGBA, gl.RGBA, gl.UNSIGNED_BYTE, textCanvas);\ngl.drawArrays(gl.TRIANGLES, 0, 3);\n```\n\n## Behavior contract\n\n- Runtime: Raw WebGL + Canvas 2D\n- Passes: 2 — Canvas 2D boot texture + raw WebGL CRT composite\n- Interaction: Customizable boot speed, CRT motion, hue, brightness, and opacity\n- Assets: No external assets\n- **renderer** (host): Raw WebGL + Canvas 2D\n- **boot** (fixed): 19 authored terminal rows\n- **assets** (fixed): None\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "energy-orb": "---\nname: add-energy-orb\ndescription: \"Build Globe from its verified Community source with the complete renderer, interactions, controls, and required assets.\"\n---\n\n# Build Globe\n\nThe original layered FBM energy sphere with translucent rim glow and depth-aware star field.\n\n## Verified source material\n\n- `NXA-Decentralized-AI-Compute-Protocol (2).html — WebGL energy orb`\n- `src/shaders/globe/GlobeCollection.tsx`\n\nSource revision: `SHA-256 03b1b8e2c440 / 3de7fdcb6239 / 7a88f26e4d8`\n\n## Requirements\n\n- Preserve the complete Raw WebGL + Canvas 2D renderer and every published Community option.\n- Preserve its interaction contract: Pointer-responsive energy sphere with visibility-aware animation and customizable motion, scale, palette, and opacity.\n- Preserve its assets: No runtime assets.\n- Verify resize, mobile, reduced motion, visibility, teardown, and console health.\n",
  "spark-badge": "---\nname: add-spark-badge\ndescription: \"Build Spark Badge from its verified Community source with the complete renderer, interactions, controls, and required assets.\"\n---\n\n# Build Spark Badge\n\nA luminous credential badge held together by curl-noise embers, carved typography, rain occlusion, waterline sparks, and an adaptive particle field.\n\n## Verified source material\n\n- `spark-badge.html — complete self-contained Canvas 2D particle scene`\n- `src/shaders/spark-badge/spark-badge.html`\n- `src/shaders/spark-badge/SparkBadge.tsx`\n\nSource revision: `SHA-256 b7c958b2a44a`\n\n## Requirements\n\n- Preserve the complete Canvas 2D renderer and every published Community option.\n- Preserve its interaction contract: Ambient 17-second dissolve/reform cycle with reduced-motion and adaptive load governor.\n- Preserve its assets: One self-contained authored HTML badge scene; no external assets.\n- Verify resize, mobile, reduced motion, visibility, teardown, and console health.\n",
  "hypnotic-loops": "---\nname: add-hypnotic-loops\ndescription: \"Build Hypnotic Loops from its verified authored source using Raw WebGL + Canvas 2D, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Hypnotic Loops\n\n## Description\n\nFour supersampled loop studies—Lines, Dots, Rays, and Type—folded across a centered surface in an orange-to-sunset palette.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React iframe host\n- Exact self-contained raw WebGL source\n- Runtime source adapter\n- Four authored polar-mesh pattern modes\n- Seven-pass brightness, blur, and bloom composite\n\n## Verified source material\n\n- `hypnotic-loops.html — complete self-contained WebGL composition`\n- `src/shaders/hypnotic-loops/hypnotic-loops.html`\n- `src/shaders/hypnotic-loops/HypnoticLoops.tsx`\n\nSource revision: `SHA-256 194f67d0a375`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Keep the complete source document byte-for-byte intact as the provenance reference.\n3. Build a runtime srcDoc that removes the four-option thumbnail rail, centers the authored polar mesh, and selects Lines, Dots, Rays, or Type through one pattern-mode uniform.\n4. Use the supplied solar-orange, coral-sunset, amber, and deep-sunset backgrounds or pass a custom hex background color.\n5. Send variant, background, and shape updates through postMessage so the iframe does not reload when controls change.\n6. Let the adapted document retain the authored WebGL2/WebGL fallback, supersampling, bloom, resize, visibility, and reduced-motion lifecycle.\n7. Unmount the iframe when its host is offscreen or the document is hidden so its WebGL context and framebuffers are released.\n8. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: Copy the exact self-contained hypnotic-loops.html document to the public URL passed through sourceUrl.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { HypnoticLoops } from \"./effects/hypnotic-loops/HypnoticLoops\";\nimport \"./effects/hypnotic-loops/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><HypnoticLoops variant=\"lines\" sourceUrl=\"/hypnotic-loops.html\" /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<HypnoticLoops variant=\"type\" background=\"#c7432f\" shape={1} />\n```\n\n## Behavior contract\n\n- Runtime: Raw WebGL + Canvas 2D\n- Passes: 7 — scene, brightness, four separable blur passes, composite\n- Interaction: Live shape deformation with four authored pattern modes, custom background color, and reduced-motion support\n- Assets: One exact self-contained authored HTML scene; no external assets\n- **renderer** (host): Raw WebGL + Canvas 2D\n- **mesh** (fixed): 63,571 polar vertices\n- **patterns** (variant): Lines | Dots | Rays | Type\n- **palette** (controlled): Orange + Sunset\n- **passes** (fixed): 7\n- **motion** (adaptive): Visibility + reduced motion\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "at-the-horizon": "---\nname: add-at-the-horizon\ndescription: \"Build Noise Flow from its verified authored source using Raw WebGL + Canvas 2D, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Noise Flow\n\n## Description\n\nA traced encounter between a luminous figure and a vast dissolving profile, rendered entirely in directional threshold grain.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React sandbox host\n- Exact self-contained raw WebGL and Canvas 2D document\n- Embedded traced silhouette, flow, and density data\n- Procedural plate construction and directional threshold dither\n- Intersection and document-visibility lifecycle\n\n## Verified source material\n\n- `at-the-horizon-we-meet.html — complete self-contained procedural plate and animated threshold dither`\n- `src/shaders/at-the-horizon/at-the-horizon-we-meet.html`\n- `src/shaders/at-the-horizon/AtTheHorizon.tsx`\n\nSource revision: `SHA-256 50c021b04f83`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Keep the complete source document byte-for-byte intact so its traced geometry, embedded fields, plate construction, shaders, grain constants, and responsive fit remain exact.\n3. Import the canonical HTML as a raw string and load it through an allow-scripts-only srcDoc iframe so the component does not depend on a public asset path.\n4. Let the authored document own its WebGL/Canvas fallback, 25 FPS grain cadence, resize rebuild, high-DPI cap, and reduced-motion still frame.\n5. Unmount the iframe when its host is offscreen or the document is hidden so its procedural canvases, WebGL context, and animation loop are released.\n6. Fill an overflow-hidden black host and expose className only; preserve the authored monochrome presentation and uncropped center fit.\n7. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { AtTheHorizon } from \"./effects/at-the-horizon/AtTheHorizon\";\nimport \"./effects/at-the-horizon/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><AtTheHorizon /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<AtTheHorizon />\n```\n\n## Behavior contract\n\n- Runtime: Raw WebGL + Canvas 2D\n- Passes: 2 — Canvas 2D plate bake + animated WebGL threshold-dither composite\n- Interaction: Responsive uncropped fit, 25 FPS grain drift, reduced-motion still frame, and visibility-aware preview lifecycle\n- Assets: Embedded flow and density fields plus traced vector silhouettes; no external assets\n- **renderer** (host): Raw WebGL + Canvas 2D\n- **plate** (fixed): Authored figure and portrait\n- **grain** (fixed): Directional threshold dither\n- **motion** (adaptive): 25 FPS | reduced-motion still\n- **layout** (responsive): Uncropped square center fit\n- **assets** (embedded): Base64 fields + vector traces\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "elements": "---\nname: add-elements\ndescription: \"Build Elements from its verified authored source using Raw WebGL2 + Canvas 2D, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Elements\n\n## Description\n\nWater, lightning, fire, condensation, and a painterly generative tree collected as one elemental family across WebGL2 and Canvas 2D.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React variant host\n- Three focused raw WebGL2 Elemental Marks panels\n- Painterly Canvas 2D Generative Tree\n- Existing transparent Canvas 2D Condensation renderer\n- Allow-scripts-only source sandbox\n- Lazy-loaded variant boundaries\n\n## Verified source material\n\n- `elemental-marks.html — complete water, lightning, and fire source`\n- `src/shaders/elements/sources/elemental-marks.html`\n- `src/shaders/elements/ElementsBackground.tsx`\n- `src/shaders/elements/ElementsCollection.tsx`\n- `generative-tree.html — complete authored Canvas 2D renderer`\n- `src/shaders/elements/sources/generative-tree.html`\n- `src/shaders/elements/GenerativeTree.tsx`\n- `src/shaders/condensation/condensationRenderer.ts`\n- `src/shaders/condensation/CondensationBackground.tsx`\n\nSource revision: `SHA-256 7a6871fe99fa`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Keep ElementsCollection as the public entry point and select water, lightning, fire, condensation, or generative-tree with the variant prop.\n3. Package the complete Elemental Marks HTML byte-for-byte, focus one authored panel, and apply the documented higher-resolution scale and detail refinements only in the React presentation adapter while preserving its mark paths and pointer behavior.\n4. Lazy-load Condensation through its existing renderer and Generative Tree through its byte-exact Canvas 2D sandbox so each lifecycle remains independent.\n5. Expose the shared speed, size, particles, opacity, and palette controls for WebGL2 marks and Generative Tree while keeping Condensation's speed, drops, and opacity controls.\n6. Mount only the selected family component and preserve sandbox, visibility, reduced-motion, resize, pointer, and cleanup lifecycles.\n7. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { ElementsCollection } from \"./effects/elements/ElementsCollection\";\nimport \"./effects/elements/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><ElementsCollection /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<ElementsCollection variant=\"lightning\" speed={1.1} particleAmount={1.2} />\n```\n\n## Behavior contract\n\n- Runtime: Raw WebGL2 + Canvas 2D\n- Passes: 1 selected composition — up to 3 WebGL2 passes or 1 Canvas 2D pass\n- Interaction: Variant selection, pointer-reactive marks and tree wind, speed, scale, particles, palette, and opacity\n- Assets: Three embedded vector brand paths; no external binary assets\n- **renderer** (host): Sandboxed WebGL2 or Canvas 2D\n- **variants** (fixed): Water + Lightning + Fire + Condensation + Generative Tree\n- **source** (fixed): Complete authored Elemental Marks + Generative Tree documents\n- **assets** (embedded): Three vector mark paths; no external tree assets\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "typography-vortex": "---\nname: add-typography-vortex\ndescription: \"Build Typography Vortex from its verified authored source using Canvas 2D, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Typography Vortex\n\n## Description\n\nSable’s complete rotating typography vortex with crisp prerendered rings, drifting glyphs, pointer dissolution, particle dust, and click suction — with dark and light surfaces.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React lifecycle host and two layered Canvas 2D surfaces\n- Dark/light mode surfaces with rebuilt glyph-ring bitmaps\n- Seeded prerendered character-ring bitmaps with adaptive pixel ratio\n- Pointer-relative alpha-mask dissolution and sampled glyph-particle emission\n- Ambient dust, click suction, visibility pausing, reduced motion, and ResizeObserver sizing\n- The exact embedded Fragment Mono font extracted as a local owned asset\n\n## Verified source material\n\n- `ascii-page-transition-v1.html — exact right-side typography vortex`\n- `src/shaders/typography-vortex/typographyVortexRenderer.ts`\n- `src/shaders/typography-vortex/TypographyVortexCanvas.tsx`\n- `src/shaders/fonts/fragment-mono.woff2`\n\nSource revision: `5a736cd3c1f6f19802f61ebb10e1701b9f7aa26e`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Load the exact Fragment Mono asset before measuring or prerendering character rings.\n3. Build seeded rings using the authored phrase, font sizing, sparse-ring cadence, alpha, angular speed, wobble, and 1.21 radius growth.\n4. Draw the rings and stray glyphs to an offscreen layer, then mask it with the authored ambient-left fade and pointer radial dissolve before compositing.\n5. Sample visible layer pixels to emit matching dust and preserve both the ambient drift and pointer burst particle systems.\n6. On pointer down, redirect existing particles into the complete timed suction state and retain the authored guide-circle feedback.\n7. Keep adaptive DPR, resize rebuilds, offscreen pausing, visibility handling, reduced motion, and complete listener/observer/frame cleanup.\n8. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: Copy the exact extracted Fragment Mono file and update the scoped @font-face URL only if the local asset directory changes.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { TypographyVortexCanvas } from \"./effects/typography-vortex/TypographyVortexCanvas\";\nimport \"./effects/typography-vortex/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><TypographyVortexCanvas speed={1} ringGrowth={1.21} /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\nconst textLayer = document.createElement(\"canvas\");\nconst text = textLayer.getContext(\"2d\", { willReadFrequently: true });\n\ndrawPrerenderedRings(text, rings, time);\napplyAmbientAndPointerDissolve(text, pointer);\ncontext.drawImage(textLayer, 0, 0, width, height);\nupdateSampledDust(context, particles, suction);\n```\n\n## Behavior contract\n\n- Runtime: Canvas 2D\n- Passes: 2 Canvas 2D passes — text layer + particle composite\n- Interaction: Mode, pointer dissolve, ambient dust, click suction, responsive rings, and reduced motion\n- Assets: Exact embedded Fragment Mono font extracted from the authored source\n- **renderer** (host): Canvas 2D\n- **mode** (optional): dark | light\n- **rings** (seeded): 1.21 growth\n- **interaction** (pointer): Dissolve + suction\n- **pixelRatio** (adaptive): ≤ 2\n- **asset** (owned): Fragment Mono\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "semantic-bloom": "---\nname: add-semantic-bloom\ndescription: \"Build Semantic Bloom from its verified authored source using Canvas 2D + DOM/CSS, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Semantic Bloom\n\n## Description\n\nA customizable Codex wordmark that draws a viscous particle organism toward its letters, illuminating the text as the network searches and reconnects.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React and TypeScript visibility-aware iframe host\n- Byte-exact Organic Semantic Explorer HTML source\n- Canvas 2D particle network with SVG goo and displacement filters\n- Customizable DOM wordmark synchronized through postMessage\n- Dark/light surfaces, pointer attraction, and reduced-motion handling\n\n## Verified source material\n\n- `design-f0ebbe02-7d8a-41fd-9041-a1124185c27b.html — complete Organic Semantic Explorer source`\n- `src/shaders/semantic-bloom/sources/design-f0ebbe02-7d8a-41fd-9041-a1124185c27b.html`\n- `src/shaders/semantic-bloom/SemanticBloom.tsx`\n\nSource revision: `SHA-256 0e48ec9ed2c3`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Keep the complete selected HTML document byte-for-byte as the canonical provenance source.\n3. Inject presentation-only CSS that hides the journal chrome and centers the editable semantic text without rewriting its particle engine.\n4. Replace only the source particle and connection colors when light mode is selected; retain the authored dark values as exact defaults.\n5. Send text, scale, and pause state through postMessage so interactive controls do not remount the iframe or reset the organism.\n6. Pause animation when the host is offscreen or the document is hidden, and render a still frame when reduced motion is requested.\n7. Keep the iframe allow-scripts-only so unmounting releases the document, canvas, listeners, filters, and animation frame together.\n8. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { SemanticBloom } from \"./effects/semantic-bloom/SemanticBloom\";\nimport \"./effects/semantic-bloom/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><SemanticBloom /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<SemanticBloom text=\"Codex\" mode=\"dark\" size={1} opacity={1} />\n```\n\n## Behavior contract\n\n- Runtime: Canvas 2D + DOM/CSS\n- Passes: 1 filtered Canvas 2D particle network over a DOM wordmark\n- Interaction: Pointer attraction, live text and size controls, synchronized light/dark mode, visibility pausing, and reduced-motion still frame\n- Assets: No external assets\n- **source** (fixed): Exact owner-selected HTML\n- **focus** (host): Centered semantic wordmark\n- **text** (optional): Codex\n- **mode** (optional): dark | light\n- **size** (optional): 0.55–1.6\n- **motion** (adaptive): Pointer + reduced-motion still\n- **assets** (fixed): None\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "globe-study": "---\nname: add-globe-study\ndescription: \"Build Text Path Studies from its verified authored source using Canvas 2D, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Text Path Studies\n\n## Description\n\nSix interactive Canvas 2D typography studies spanning a globe, flowing outlines, morphing glyphs, cloth physics, ripples, and a particle sphere.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React variant host\n- Two exact authored Canvas 2D documents\n- Six interactive text-path studies\n- Sandboxed source isolation\n- Light and dark palette controls\n\n## Verified source material\n\n- `text-on-a-path-ii.html — FIG 06 Globe, FIG 07 Outline Typeflow, FIG 08 Morphing Glyph Cloud, and FIG 11 Cloth`\n- `text-on-a-path.html — FIG 04 Ripple and FIG 05 Ball`\n- `src/shaders/text-path-studies/TextPathStudies.tsx`\n\nSource revision: `SHA-256 2e21ae3b77c3 + e5d01ff0fc47`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Keep TextPathStudies as the public entry point and select globe-study, outline-typeflow, morphing-glyph-cloud, cloth-study, ripple-study, or ball-study with the variant prop.\n3. Retain both complete authored Text on a Path documents and focus only the selected figure inside an allow-scripts-only sandbox.\n4. Preserve each study's own drag, zoom, hover, click, morph, cloth, ripple, or particle interaction instead of flattening them into one renderer.\n5. Expose shared mode, scale, opacity, and palette controls at the collection boundary.\n6. Mount only the selected source document so inactive studies do not allocate canvases or animation loops.\n7. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { TextPathStudies } from \"./effects/globe-study/TextPathStudies\";\nimport \"./effects/globe-study/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><TextPathStudies /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<TextPathStudies variant=\"outline-typeflow\" mode=\"dark\" />\n```\n\n## Behavior contract\n\n- Runtime: Canvas 2D\n- Passes: 1 selected sandboxed Canvas 2D study\n- Interaction: Variant-specific drag, zoom, hover, click, morph, cloth, ripple, and particle interactions\n- Assets: No external assets\n- **source** (fixed): Six exact authored figures across two documents\n- **renderer** (sandbox): Canvas 2D\n- **mode** (optional): dark | light\n- **variant** (optional): Six text-path studies\n- **pixelRatio** (adaptive): ≤ 2.5\n- **assets** (fixed): None\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "gradient-collection": "---\nname: add-gradient-collection\ndescription: \"Build Gradient Collection from its verified authored source using Canvas 2D, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Gradient Collection\n\n## Description\n\nFour directional twelve-tile gradient carousels orbiting kinetic ThreeUI headlines, with preserved perspective, shadows, and a synchronized light/dark canvas.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React component with a sandboxed `srcDoc` effect boundary\n- Canvas 2D copied from the byte-exact Neuform export\n- A post-load focus adapter that keeps only the authored shader, button, canvas, or visual targets visible\n- Optional outer-frame hue, saturation, and brightness grading with source-exact defaults\n\n## Verified source material\n\n- `src/shaders/neuform-isolated/sources/gradient-collection.html`\n- `src/shaders/neuform-isolated/NeuformIsolatedEffects.tsx`\n\nSource revision: `SHA-256 8e42d2d5b497`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Copy the complete canonical HTML source byte-for-byte so its shader strings, materials, DOM, timing, and initialization order remain auditable.\n3. After the source load event, retain only the `#stage` twelve-tile gradient carousel and rebranded launch typography; do not rewrite the renderer or approximate the composition.\n4. Force retained background targets to the sandbox viewport and center retained buttons without changing their internal pointer or shader state.\n5. Dispatch one resize event after reparenting so the exact source renderer recalculates its backing resolution.\n6. Apply optional hue, saturation, and brightness only to the outer iframe; omit the filter at 0/1/1 so source color remains exact.\n7. Keep the sandbox isolated with `allow-scripts` only; removing the iframe must release its document, listeners, frames, and graphics contexts together.\n8. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { GradientCollection } from \"./effects/gradient-collection/GradientCollection\";\nimport \"./effects/gradient-collection/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><GradientCollection /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\nconst focusedSource = canonicalHtml\n  .replace(\"</head>\", focusStyles + \"</head>\")\n  .replace(\"</body>\", focusAfterLoadScript + \"</body>\");\n\nreturn <iframe title=\"Focused source effect\" srcDoc={focusedSource} sandbox=\"allow-scripts\" />;\n```\n\n## Behavior contract\n\n- Runtime: Canvas 2D\n- Passes: 1 Canvas 2D typographic carousel\n- Interaction: Four rising, falling, horizontal, and vertical compositions complete one orbit every 15 seconds with synchronized light/dark mode\n- Assets: No external assets\n- **source** (fixed): Exact owner-selected HTML\n- **focus** (host): Effect-only sandbox\n- **mode** (optional): dark | light\n- **palette** (optional): Final-frame grade\n- **assets** (fixed): No owned binary assets\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "star-portal": "---\nname: add-star-portal\ndescription: \"Build Shader Buttons from its verified authored source using Raw WebGL + Canvas 2D + CSS, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Shader Buttons\n\n## Description\n\nSix authored shader and canvas button treatments collected into one interactive family.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React variant host\n- Three raw WebGL sources\n- Three Canvas 2D sources\n- Authored CSS controls\n- Lazy-loaded isolated renderers\n\n## Verified source material\n\n- `src/shaders/shader-buttons/ShaderButtons.tsx`\n- `src/shaders/neuform-isolated/NeuformIsolatedEffects.tsx`\n- `src/shaders/neuform-isolated/sources/imaginie-starfield.html`\n- `src/shaders/neuform-isolated/sources/ignition-terminal.html`\n- `src/shaders/neuform-isolated/sources/valence-core.html`\n- `src/shaders/neuform-isolated/sources/aetheris-labs.html`\n- `src/shaders/neuform-isolated/sources/nexus-tactile.html`\n- `src/shaders/neuform-isolated/sources/uploading-button.html`\n\nSource revision: `SHA-256 002199215e12`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Keep ShaderButtons as the public entry point and select star-portal, ignition-button, induction-button, plasma-button, tactile-button, or uploading-button with the variant prop.\n3. Retain all six complete authored documents and their independent shader, canvas, CSS, resize, and interaction systems.\n4. Expose mode and palette controls at the family boundary while leaving each button's authored behavior intact.\n5. Lazy-load and mount only the selected button renderer so inactive variants do not allocate canvases, WebGL contexts, or animation loops.\n6. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { ShaderButtons } from \"./effects/star-portal/ShaderButtons\";\nimport \"./effects/star-portal/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><ShaderButtons /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<ShaderButtons variant=\"plasma-button\" mode=\"dark\" />\n```\n\n## Behavior contract\n\n- Runtime: Raw WebGL + Canvas 2D + CSS\n- Passes: 1 selected shader, canvas, and CSS button composition\n- Interaction: Variant selection with authored pointer, hover, motion, and palette behavior\n- Assets: No owned binary assets\n- **source** (fixed): Exact Neuform HTML\n- **focus** (host): Effect-only sandbox\n- **mode** (optional): dark | light\n- **palette** (optional): Final-frame grade\n- **assets** (fixed): No owned binary assets\n- **variants** (fixed): Star Portal + Ignition + Induction + Plasma + Tactile + Thinking\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "particle-orb": "---\nname: add-particle-orb\ndescription: \"Build Particle Orb from its verified authored source using Canvas 2D, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Particle Orb\n\n## Description\n\nThe exact autonomous-system particle orb and connective field isolated from its source page.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React component with a sandboxed `srcDoc` effect boundary\n- Canvas 2D particle renderer copied from the byte-exact Neuform export\n- A post-load focus adapter that keeps only the authored shader, button, canvas, or visual targets visible\n- Optional outer-frame hue, saturation, and brightness grading with source-exact defaults\n\n## Verified source material\n\n- `src/shaders/neuform-isolated/sources/synthesis-orb.html`\n- `src/shaders/neuform-isolated/NeuformIsolatedEffects.tsx`\n\nSource revision: `SHA-256 4acc5f965c2c`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Copy the complete canonical HTML source byte-for-byte so its shader strings, materials, DOM, timing, and initialization order remain auditable.\n3. After the source load event, retain only the `#orbCanvas` renderer; do not rewrite the renderer or approximate the composition.\n4. Force retained background targets to the sandbox viewport and center retained buttons without changing their internal pointer or shader state.\n5. Dispatch one resize event after reparenting so the exact source renderer recalculates its backing resolution.\n6. Apply optional hue, saturation, and brightness only to the outer iframe; omit the filter at 0/1/1 so source color remains exact.\n7. Keep the sandbox isolated with `allow-scripts` only; removing the iframe must release its document, listeners, frames, and graphics contexts together.\n8. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { ParticleOrbField } from \"./effects/particle-orb/ParticleOrbField\";\nimport \"./effects/particle-orb/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><ParticleOrbField /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\nconst focusedSource = canonicalHtml\n  .replace(\"</head>\", focusStyles + \"</head>\")\n  .replace(\"</body>\", focusAfterLoadScript + \"</body>\");\n\nreturn <iframe title=\"Focused source effect\" srcDoc={focusedSource} sandbox=\"allow-scripts\" />;\n```\n\n## Behavior contract\n\n- Runtime: Canvas 2D\n- Passes: 1 Canvas 2D particle-network pass\n- Interaction: Autonomous particle motion + optional final-frame palette\n- Assets: No owned binary assets\n- **source** (fixed): Exact Neuform HTML\n- **focus** (host): Effect-only sandbox\n- **palette** (optional): Final-frame grade\n- **assets** (fixed): No owned binary assets\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "maccess-glass-button": "---\nname: add-maccess-glass-button\ndescription: \"Build Rectangle Buttons from its verified authored source using DOM + CSS, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Rectangle Buttons\n\n## Description\n\nThirteen authored rectangle-button and animated CTA treatments collected into one family.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React variant host\n- One Maccess DOM/CSS source\n- Ten isolated DOM/CSS CTA sources\n- Two Lumen DOM/CSS treatments\n- Light and dark palette controls\n- Lazy-loaded variant renderers\n\n## Verified source material\n\n- `src/shaders/rectangle-buttons/RectangleButtons.tsx`\n- `maccess.html — shared button treatment`\n- `src/shaders/maccess-elements/MaccessElements.tsx`\n- `src/shaders/maccess-elements/maccess-elements.css`\n- `src/shaders/neuform-isolated/NeuformIsolatedEffects.tsx`\n- `src/shaders/neuform-isolated/sources/launch-button.html`\n- `src/shaders/neuform-isolated/sources/dot-border-button.html`\n- `src/shaders/neuform-isolated/sources/floating-dots-cta.html`\n- `src/shaders/neuform-isolated/sources/sliding-text-cta.html`\n- `src/shaders/neuform-isolated/sources/gradient-beam-cta.html`\n- `src/shaders/neuform-isolated/sources/gradient-pill-button.html`\n- `src/shaders/neuform-isolated/sources/generate-button.html`\n- `src/shaders/neuform-isolated/sources/glassmorphism-cta.html`\n- `src/shaders/neuform-isolated/sources/spinning-border-button.html`\n- `src/shaders/neuform-isolated/sources/gradient-cta.html`\n- `src/shaders/lumen-cta/LumenCta.tsx`\n- `src/shaders/lumen-cta/lumen-cta.css`\n- `src/shaders/lumen-cta/sources/lumen.html`\n\nSource revision: `SHA-256 91315de0cfb8`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Keep RectangleButtons as the public entry point and select dark-pill, launch-button, dot-border-button, floating-dots-cta, sliding-text-cta, gradient-beam-cta, gradient-pill-button, generate-button, glassmorphism-cta, spinning-border-button, gradient-cta, lumen-cta, or lumen-cta-ghost with the variant prop.\n3. Retain the original dark-glass rectangle, all ten complete authored CTA documents, and both Lumen treatments instead of flattening their markup or animation systems.\n4. Expose mode and palette controls at the collection boundary while leaving each variant's authored hover, focus, and motion behavior intact.\n5. Lazy-load and mount only the selected CTA renderer so inactive variants do not allocate isolated documents or animation loops.\n6. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { RectangleButtons } from \"./effects/maccess-glass-button/RectangleButtons\";\nimport \"./effects/maccess-glass-button/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><RectangleButtons /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<RectangleButtons variant=\"gradient-beam-cta\" mode=\"dark\" />\n```\n\n## Behavior contract\n\n- Runtime: DOM + CSS\n- Passes: 1 selected DOM/CSS button composition\n- Interaction: Variant selection with authored hover, focus, motion, and palette behavior\n- Assets: 5 local SF Pro font subsets + 1 authored remote portrait reference\n- **renderer** (host): React DOM + scoped CSS\n- **source** (fixed): Owner-selected reference HTML\n- **theme** (fixed): Dark\n- **layout** (responsive): Container-relative 16:9 composition\n- **motion** (adaptive): Reduced-motion safe\n- **assets** (owned): Local fonts and illustrations\n- **variants** (fixed): Dark Glass + Launch + Dot Border + Floating Dots + Sliding Text + Gradient Beam + Gradient Pill + Generate + Glassmorphism + Spinning Border + Gradient + Lumen CTA + Lumen CTA Ghost\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "circle-buttons": "---\nname: add-circle-buttons\ndescription: \"Build Circle Buttons from its verified authored source using DOM + CSS, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Circle Buttons\n\n## Description\n\nThree compact circular icon controls using the exact Dark Glass, Launch, and Dot Border material systems.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React and TypeScript\n- Semantic button elements\n- Scoped layered CSS\n- Inline SVG icons\n- Light and dark palette controls\n- Reduced-motion fallback\n\n## Verified source material\n\n- `src/shaders/circle-buttons/CircleButtons.tsx`\n- `src/shaders/circle-buttons/circle-buttons.css`\n\nSource revision: `SHA-256 2e85693f7ada`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Use CircleButtons as the public entry point and select play, plus, or mail with the variant prop.\n3. Keep every variant circular, icon-only, and compact at its responsive 56–72px default; provide ariaLabel when the surrounding action needs a more specific accessible name.\n4. Map each treatment directly to its Rectangle Buttons source: Dark Glass for Play, Launch for Plus, and Dot Border for Mail, including the original material tokens and motion timing.\n5. Keep hover, keyboard focus, pressed, disabled, and reduced-motion behavior intact when adapting the control to another layout.\n6. Use mode and palette controls at the component boundary rather than rewriting the internal highlight and shadow layers.\n7. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { CircleButtons } from \"./effects/circle-buttons/CircleButtons\";\nimport \"./effects/circle-buttons/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><CircleButtons /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<CircleButtons variant=\"mail\" mode=\"dark\" ariaLabel=\"Open inbox\" />\n```\n\n## Behavior contract\n\n- Runtime: DOM + CSS\n- Passes: 1 layered DOM/CSS circle composition\n- Interaction: Source-faithful hover and press behavior, focus-visible ring, reduced-motion fallback, and adaptive light/dark palette\n- Assets: Inline SVG icons; no external runtime assets\n- **renderer** (host): Semantic button + scoped layered CSS\n- **variants** (fixed): Play + Plus + Mail\n- **materials** (fixed): Dark Glass + Launch + Dot Border\n- **size** (responsive): 56–72px diameter\n- **theme** (adaptive): Dark (default) | Light\n- **interaction** (adaptive): Hover | Focus | Press | Disabled | Reduced motion\n- **assets** (embedded): Three inline SVG icons\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "liquid-metal-button": "---\nname: add-liquid-metal-button\ndescription: \"Build Liquid Metal Button from its verified authored source using Raw WebGL 2 + DOM/CSS, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Liquid Metal Button\n\n## Description\n\nA prismatic liquid-metal control in Sign up pill, Liquid Orb, and configurable Play Circle variants, with pointer-following bloom and press ripples.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React and TypeScript visibility-aware sandbox host\n- Exact self-contained raw WebGL2, DOM, and CSS document imported as source text\n- Semantic native button with authored hover, focus, pressed, and keyboard states\n- Multipass spectral metal, crisp rim, adaptive softening, multi-radius bloom, and composite pipeline\n- Configurable circular play variant with live finish, diameter, stroke, and accessible-label controls\n- Pointer-dragged liquid field, faceted press ripples, capped DPR, and reduced-motion freezing\n\n## Verified source material\n\n- `src/shaders/liquid-metal-button/liquid-metal-button.html`\n- `src/shaders/liquid-metal-button/LiquidMetalButton.tsx`\n\nSource revision: `SHA-256 76624e881a3a`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Keep the complete authored HTML byte-for-byte; do not rewrite its shaders, framebuffer graph, CSS plate, button semantics, interaction state, or tuning hooks.\n3. Import the canonical document as a raw string and mount it through an `allow-scripts`-only srcDoc iframe so its WebGL2 and DOM event scopes remain isolated.\n4. Select the authored Sign up pill with `variant=\"pill\"`, the compact Liquid Orb with `variant=\"circle\"`, or the configurable play control with `variant=\"play\"`.\n5. Let the source-owned CSS size the pill responsively, keep the orb at 56–72px, and let the renderer cap backing resolution at 2× device pixel ratio.\n6. Select the play variant to map equal width and height into the same pill SDF, then update diameter, rim width, colored or monotone finish, and the icon-only control's accessible name through the iframe message adapter without remounting it.\n7. Retain pointer hover and drag, pointer and keyboard ripple launches, focus-visible styling, and the source-owned reduced-motion clock freeze.\n8. Unmount the iframe when the host is offscreen or the document is hidden so its WebGL context, buffers, framebuffers, listeners, and animation loop are released together.\n9. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { LiquidMetalButton } from \"./effects/liquid-metal-button/LiquidMetalButton\";\nimport \"./effects/liquid-metal-button/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><LiquidMetalButton /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<LiquidMetalButton variant=\"play\" rendering=\"colored\" diameter={88} strokeWidth={3} text=\"Play\" />\n```\n\n## Behavior contract\n\n- Runtime: Raw WebGL 2 + DOM/CSS\n- Passes: Up to 20 — metal, crisp rim, adaptive softening, multi-radius bloom, and composite\n- Interaction: Hover, focus, pointer-dragged metal, faceted press ripples, and Enter/Space activation\n- Assets: 1 exact authored HTML scene with remote Inter stylesheet and system-font fallback\n- **renderer** (sandbox): Raw WebGL 2 + DOM/CSS\n- **control** (semantic): Native button\n- **variants** (fixed): Sign up Pill + Liquid Orb + configurable Play Circle\n- **geometry** (optional): 72–160 px circle + 1–8 px stroke\n- **appearance** (optional): Colored | monotone\n- **content** (optional): Custom accessible name\n- **metal** (fixed): Spectral dispersion field\n- **post** (adaptive): Softening + bloom\n- **interaction** (pointer): Hover + drag + ripple\n- **pixelRatio** (adaptive): ≤ 2\n- **motion** (adaptive): Reduced-motion freeze\n- **assets** (external): Inter stylesheet + fallback\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "maccess-workflow": "---\nname: add-maccess-workflow\ndescription: \"Build Sections from its verified authored source using DOM + CSS, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Sections\n\n## Description\n\nThree reusable dark product sections collected into one responsive family.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React variant host\n- Scoped DOM and CSS\n- Six authored SVG illustration layers\n- Neutral product copy\n- Lazy-loaded variant renderers\n\n## Verified source material\n\n- `src/shaders/sections/SectionsCollection.tsx`\n- `src/shaders/maccess-elements/MaccessElements.tsx`\n- `src/shaders/maccess-elements/maccess-elements.css`\n- `Original HTML — layout and motion source`\n\nSource revision: `SHA-256 91315de0cfb8`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Keep SectionsCollection as the public entry point and select workflow, editorial-intro, or newsletter-footer with the variant prop.\n3. Preserve the three responsive compositions, device illustrations, form behavior, and reduced-motion rules while keeping the content neutral and reusable.\n4. Keep source product icons, wordmarks, company lines, product names, and subscription labels out of rendered and copied output.\n5. Lazy-load and mount only the selected composition so inactive variants do not allocate their animations or form state.\n6. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { SectionsCollection } from \"./effects/maccess-workflow/SectionsCollection\";\nimport \"./effects/maccess-workflow/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><SectionsCollection /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<SectionsCollection variant=\"newsletter-footer\" />\n```\n\n## Behavior contract\n\n- Runtime: DOM + CSS\n- Passes: 1 selected responsive DOM/SVG composition\n- Interaction: Variant selection with authored motion, native form behavior, and reduced-motion fallback\n- Assets: 5 local font subsets + 6 authored SVG illustrations\n- **renderer** (host): React DOM + scoped CSS\n- **source** (fixed): Owner-selected reference HTML\n- **theme** (fixed): Dark\n- **layout** (responsive): Container-relative 16:9 composition\n- **motion** (adaptive): Reduced-motion safe\n- **assets** (owned): Local fonts and illustrations\n- **identity** (fixed): Neutral, unbranded copy\n- **variants** (fixed): Onboarding Steps + Editorial Intro + Newsletter Footer\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "character-carousel": "---\nname: add-character-carousel\ndescription: \"Build Character Carousel from its verified authored source using DOM + CSS, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Character Carousel\n\n## Description\n\nTwo authored editorial character-card carousels collected as a light filmstrip and a dark responsive wave.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React variant host\n- Two byte-exact authored DOM/CSS documents\n- Embedded portrait JPEGs\n- Sandboxed source isolation\n- Pointer, wheel, keyboard, palette, and lifecycle controls\n\n## Verified source material\n\n- `character-filmstrip.html — complete authored light filmstrip`\n- `character-wave.html — complete authored dark wave`\n- `src/shaders/character-carousel/sources/character-filmstrip.html`\n- `src/shaders/character-carousel/sources/character-wave.html`\n- `src/shaders/character-carousel/CharacterCarousel.tsx`\n\nSource revision: `SHA-256 4c98939e0e2b`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Keep CharacterCarousel as the public entry point and select filmstrip or wave with the variant prop.\n3. Retain both complete source documents byte-for-byte, including their embedded portraits, card labels, paper or grain surfaces, looping geometry, and responsive breakpoints.\n4. Preserve the filmstrip pointer, wheel, arrow, focus, and idle-drift behavior plus the wave orientation toggle and horizontal-to-vertical responsive transition.\n5. Expose speed and scale through a postMessage adapter and apply opacity and palette grading only at the outer iframe boundary so defaults remain source-exact.\n6. Keep allow-scripts as the only sandbox permission and pause the authored loop whenever the host or document is hidden.\n7. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { CharacterCarousel } from \"./effects/character-carousel/CharacterCarousel\";\nimport \"./effects/character-carousel/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><CharacterCarousel /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<CharacterCarousel variant=\"wave\" speed={1} scale={1} />\n```\n\n## Behavior contract\n\n- Runtime: DOM + CSS\n- Passes: 1 selected responsive DOM/CSS card composition\n- Interaction: Pointer focus, wheel and arrow navigation, card selection, idle drift, responsive orientation, scale, speed, opacity, and palette\n- Assets: 4 embedded authored portrait JPEGs shared by both exact source documents\n- **renderer** (host): Sandboxed authored DOM/CSS documents\n- **variant** (prop): \"filmstrip\" | \"wave\"\n- **interaction** (input): pointer + wheel + keyboard + card focus\n- **lifecycle** (host): responsive + visibility-aware + reduced motion\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "gallery": "---\nname: add-gallery\ndescription: \"Build Gallery from its verified authored source using Three.js r149, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Gallery\n\n## Description\n\nThe isolated Vantrix hero image ribbon: sixteen curved editorial panels orbiting a vertical cylindrical rail on a quiet paper grid.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React\n- Three.js r149\n- Scoped CSS\n- CylinderGeometry image ribbon\n- Five local WebP textures\n- Responsive and visibility-aware renderer lifecycle\n\n## Verified source material\n\n- `Vantrix Studio Landing Page Template.html — hero carousel only`\n- `src/shaders/gallery/Gallery.tsx`\n- `src/shaders/gallery/gallery.css`\n- `src/shaders/gallery/assets/gallery-1.webp`\n- `src/shaders/gallery/assets/gallery-2.webp`\n- `src/shaders/gallery/assets/gallery-3.webp`\n- `src/shaders/gallery/assets/gallery-4.webp`\n- `src/shaders/gallery/assets/gallery-5.webp`\n\nSource revision: `SHA-256 f7a576d4db08`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Use Gallery as a standalone UI element and keep the extraction limited to the Vantrix hero image ribbon.\n3. Do not include the source navigation, loader, cursor, marketing cards, headings, calls to action, or lower landing-page sections.\n4. Preserve the 35-degree camera, 16 curved cylindrical panels, four-turn placement, slow rotation, and vertical drift from the authored hero.\n5. Keep all five gallery textures local and use speed, scale, opacity, and final-frame palette controls at the component boundary.\n6. Retain DPR-capped resizing, hidden-host suspension, reduced-motion rendering, and complete Three.js disposal.\n7. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: Copy all five local gallery WebP textures beside Gallery.tsx and keep the new URL asset references aligned if their location changes.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { Gallery } from \"./effects/gallery/Gallery\";\nimport \"./effects/gallery/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><Gallery speed={1} scale={1} /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<Gallery speed={1} scale={1} />\n```\n\n## Behavior contract\n\n- Runtime: Three.js r149\n- Passes: 1 live Three.js scene with 16 textured cylindrical panels\n- Interaction: Automatic rotation and vertical drift, responsive DPR-capped resize, reduced-motion still frame, and visibility-aware lifecycle\n- Assets: 5 locally optimized authored gallery images\n- **renderer** (host): Extracted hero carousel only\n- **geometry** (fixed): 16 curved cylindrical image panels\n- **motion** (prop): speed + scale\n- **lifecycle** (host): responsive + visibility-aware + reduced motion\n- **assets** (local): 5 authored WebP textures\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "genie-dock": "---\nname: add-genie-dock\ndescription: \"Build Genie Dock from its verified authored source using DOM + SVG + CSS, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Genie Dock\n\n## Description\n\nA macOS-inspired application dock where live windows bend into their icons along a measured Genie curve, then restore without flattening their DOM content.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React sandbox host\n- Byte-exact authored HTML\n- Live DOM slicing\n- SVG silhouette and projective transforms\n- Visibility-aware lifecycle\n\n## Verified source material\n\n- `genie-effect.html — complete authored dock and window source`\n- `src/shaders/genie-dock/sources/genie-effect.html`\n- `src/shaders/genie-dock/GenieDock.tsx`\n\nSource revision: `SHA-256 721030e1dbf0`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Keep the complete genie-effect.html document byte-for-byte as the canonical source and mount it inside an allow-scripts-only iframe.\n3. Preserve the authored 20–64 live DOM slices, measured funnel profile, SVG silhouette, projective quad matrices, and 410ms window-to-dock timing.\n4. Retain dock-icon and traffic-light minimizing, minimized-window restoration, titlebar movement and downward scrubbing, window zoom, and live form synchronization.\n5. Keep the responsive cluster and dock measurements intact; pause by unmounting the iframe while the host or document is hidden.\n6. For reduced motion, keep every control and state available while resolving minimize and restore transitions effectively instantly.\n7. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: Copy the complete canonical HTML document; the Lexend variable font and fjord image are embedded, so no external asset URL is required.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { GenieDock } from \"./effects/genie-dock/GenieDock\";\nimport \"./effects/genie-dock/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><GenieDock /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<GenieDock />\n```\n\n## Behavior contract\n\n- Runtime: DOM + SVG + CSS\n- Passes: 20–64 live DOM slices per moving window + 1 SVG silhouette\n- Interaction: Click dock icons or traffic lights to minimize and restore, drag titlebars downward to scrub the curve, move and zoom windows, and edit live form controls mid-flight\n- Assets: 1 embedded Lexend variable font + 1 embedded authored fjord image\n- **renderer** (host): Sandboxed authored DOM/SVG document\n- **motion** (fixed): Measured Genie funnel with live DOM slices\n- **interaction** (input): dock + traffic lights + pointer drag + form controls\n- **lifecycle** (host): responsive + visibility-aware + reduced motion\n- **assets** (embedded): Lexend variable font + fjord image\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "recursive-erosion": "---\nname: add-recursive-erosion\ndescription: \"Build Recursive Erosion from its verified authored source using Raw WebGL + Canvas 2D, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Recursive Erosion\n\n## Description\n\nA looping particle sphere that recursively erodes into holes while luminous trails crawl across its surface.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React component with a sandboxed `srcDoc` effect boundary\n- raw WebGL and Canvas 2D copied from the byte-exact Neuform export\n- A post-load focus adapter that keeps only the authored shader, button, canvas, or visual targets visible\n- Optional outer-frame hue, saturation, and brightness grading with source-exact defaults\n\n## Verified source material\n\n- `src/shaders/neuform-isolated/sources/recursive-erosion.html`\n- `src/shaders/neuform-isolated/NeuformIsolatedEffects.tsx`\n\nSource revision: `SHA-256 2f407d606658`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Copy the complete canonical HTML source byte-for-byte so its shader strings, materials, DOM, timing, and initialization order remain auditable.\n3. After the source load event, retain only the text-free `#stage` particle sphere; do not rewrite the renderer or approximate the composition.\n4. Force retained background targets to the sandbox viewport and center retained buttons without changing their internal pointer or shader state.\n5. Dispatch one resize event after reparenting so the exact source renderer recalculates its backing resolution.\n6. Apply optional hue, saturation, and brightness only to the outer iframe; omit the filter at 0/1/1 so source color remains exact.\n7. Keep the sandbox isolated with `allow-scripts` only; removing the iframe must release its document, listeners, frames, and graphics contexts together.\n8. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { RecursiveErosionBackground } from \"./effects/recursive-erosion/RecursiveErosionBackground\";\nimport \"./effects/recursive-erosion/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><RecursiveErosionBackground /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\nconst focusedSource = canonicalHtml\n  .replace(\"</head>\", focusStyles + \"</head>\")\n  .replace(\"</body>\", focusAfterLoadScript + \"</body>\");\n\nreturn <iframe title=\"Focused source effect\" srcDoc={focusedSource} sandbox=\"allow-scripts\" />;\n```\n\n## Behavior contract\n\n- Runtime: Raw WebGL + Canvas 2D\n- Passes: 3 WebGL particle passes + 1 Canvas 2D grain composite\n- Interaction: Authored four-second seamless loop + optional final-frame palette\n- Assets: No owned binary assets\n- **source** (fixed): Exact owner-selected HTML\n- **focus** (host): Effect-only sandbox\n- **palette** (optional): Final-frame grade\n- **assets** (fixed): No owned binary assets\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "quantera-trading-background": "---\nname: add-quantera-trading-background\ndescription: \"Build Quantera Trading Background from its verified authored source using Three.js r165 + postprocessing, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Quantera Trading Background\n\n## Description\n\nA deep green trading field with volumetric light shafts, drifting chalk equations, sparkling dust, subtle bloom, and pointer parallax.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React sandbox host\n- Byte-exact authored HTML\n- Three.js r165\n- ShaderMaterial\n- CanvasTexture equations\n- UnrealBloom postprocessing\n- Visibility-aware lifecycle\n\n## Verified source material\n\n- `src/shaders/quantera-trading-background/QuanteraTradingBackground.tsx`\n- `src/shaders/quantera-trading-background/sources/quantera-trading-hero.html`\n\nSource revision: `SHA-256 ed0989b5f8f5`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Keep quantera-trading-hero.html byte-for-byte as the canonical provenance source.\n3. Present only the authored WebGL background: retain the volumetric green light shafts, drifting chalk equations, sparkle particles, bloom, and pointer parallax while hiding the navigation, headline, feature row, calls to action, social links, and scroll prompt.\n4. Keep the source Three.js r165 and postprocessing import map intact so the authored renderer and bloom pipeline remain unchanged.\n5. Cap DPR at the source value, preserve resize and adaptive-resolution behavior, and render one settled frame for reduced motion.\n6. Mount the allow-scripts-only iframe only while its host and document are visible so route changes release the renderer and animation frame together.\n7. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { QuanteraTradingBackground } from \"./effects/quantera-trading-background/QuanteraTradingBackground\";\nimport \"./effects/quantera-trading-background/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><QuanteraTradingBackground /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<QuanteraTradingBackground />\n```\n\n## Behavior contract\n\n- Runtime: Three.js r165 + postprocessing\n- Passes: 1 Three.js scene render + UnrealBloom postprocessing\n- Interaction: Pointer-driven light, equation, particle, and camera-space parallax\n- Assets: No binary scene assets — procedural ShaderMaterials, CanvasTextures, and CDN Three.js r165 modules\n- **renderer** (sandbox): Three.js r165 + bloom\n- **presentation** (fixed): Background only; page UI hidden\n- **layers** (fixed): Light shafts + equations + particles\n- **interaction** (pointer): Parallax + light response\n- **motion** (adaptive): Visibility + reduced motion\n- **pixelRatio** (adaptive): ≤ 1.75\n- **assets** (fixed): No binary scene assets\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "sylva-living-world": "---\nname: add-sylva-living-world\ndescription: \"Build Sylva Living World from its verified Community source with the complete renderer, interactions, controls, and required assets.\"\n---\n\n# Build Sylva Living World\n\nThe original procedural moss-root world with pale flowers, ferns, drifting pollen, scan light, and a landing butterfly.\n\n## Verified source material\n\n- `src/shaders/sylva-living-world/SylvaLivingWorldScene.tsx`\n- `src/shaders/sylva-living-world/sources/inner-green-3d.html`\n- `src/shaders/sylva-living-world/sources/inner-green-assets/three.min.js`\n\nSource revision: `SHA-256 fd922291297d`\n\n## Requirements\n\n- Preserve the complete Three.js r149 renderer and every published Community option.\n- Preserve its interaction contract: Pointer-driven moss parting, camera parallax, pollen trails, scan-light entrance, and butterfly flight.\n- Preserve its assets: No binary assets — the canonical HTML and local MIT Three.js runtime are carried as source.\n- Verify resize, mobile, reduced motion, visibility, teardown, and console health.\n",
  "temple-night": "---\nname: add-temple-night\ndescription: \"Build Temple Night from its verified Community source with the complete renderer, interactions, controls, and required assets.\"\n---\n\n# Build Temple Night\n\nKage’s procedural Kyoto mountain temple after dark, with the exact authored architecture, rain, mist, leaves, pointer wisps, camera composition, and bloom pipeline.\n\n## Verified source material\n\n- `kage-github/index.html — procedural Three.js world`\n- `src/shaders/temple-night/templeNightRenderer.js`\n- `src/shaders/temple-night/TempleNightScene.tsx`\n\nSource revision: `4399487d2fb42bce39c7b032fbbb50d230bf4f0b`\n\n## Requirements\n\n- Preserve the complete Three.js r149 renderer and every published Community option.\n- Preserve its interaction contract: Pointer parallax and world-space cursor wisps.\n- Preserve its assets: No external scene assets.\n- Verify resize, mobile, reduced motion, visibility, teardown, and console health.\n",
  "landscape": "---\nname: add-landscape\ndescription: \"Build Landscape from its verified Community source with the complete renderer, interactions, controls, and required assets.\"\n---\n\n# Build Landscape\n\nA tower-free procedural terrain whose light, sky, fog, stars, rain, lightning, snow, grass, and stones move through seven authored environment states.\n\n## Verified source material\n\n- `src/shaders/japanese-tower/Towers.html — exact upstream self-contained source`\n- `scripts/generate-landscape.mjs — deterministic tower-free presentation adapter`\n- `src/shaders/landscape/LandscapeScene.tsx`\n- `public/landscape.html — generated scene-only package asset`\n\nSource revision: `e8aab488464bcd44471c390cf309597c6f3adb05`\n\n## Requirements\n\n- Preserve the complete Three.js r149 renderer and every published Community option.\n- Preserve its interaction contract: Pointer parallax, drag-to-orbit, wheel and pinch zoom, with cross-fading time and weather systems.\n- Preserve its assets: No external scene assets — the exact self-contained source carries its runtime and procedural systems.\n- Verify resize, mobile, reduced motion, visibility, teardown, and console health.\n",
  "japanese-tower": "---\nname: add-japanese-tower\ndescription: \"Build Country Towers from its verified authored source using Three.js r149 + Canvas 2D, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Country Towers\n\n## Description\n\nSix country-specific towers assembling above a procedural landscape: Japanese, Chinese, Vietnamese, Thai, Khmer, and Ottoman.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React and TypeScript sandbox host\n- Self-contained Three.js r149 document with embedded runtime and texture set\n- Six procedural architectures: Japanese tenshu, Chinese pagoda, Vietnamese tháp, Thai prang, Khmer prasat, and Ottoman mosque\n- Shared heightfield terrain, mountains, grass, stones, atmosphere, and lighting\n- Construction timeline, pointer orbit, hover parallax, pinch and wheel zoom, and responsive camera fitting\n\n## Verified source material\n\n- `Towers.html — complete self-contained Three.js tower and landscape scene`\n- `src/shaders/japanese-tower/Towers.html`\n- `src/shaders/japanese-tower/JapaneseTowerLandscape.tsx`\n- `public/japanese-tower.html — generated scene-only package asset`\n\nSource revision: `SHA-256 7810e7163c02`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Copy the complete Towers.html source byte-for-byte so the embedded runtime, texture set, geometry, landscape, camera, and timeline remain intact.\n3. Inject only scene-focused presentation CSS at runtime: hide the editorial chrome while keeping the background wash, tower canvas, and weather flash layers.\n4. Map the country prop to the six authored style IDs in the generated scene asset; keep Japan as the default and disable sound for an embeddable component surface.\n5. Load the focused document in an allow-scripts-only sandbox and size the iframe to a bounded host so its native resize path preserves the tower and landscape composition.\n6. Remove the iframe on unmount so its browsing context, WebGL renderer, audio graph, listeners, and animation frame are released together.\n7. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: Generate the focused japanese-tower.html asset from the exact Towers.html source, retain the six-country query adapter, and keep it available at the URL passed through sourceUrl.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { JapaneseTowerLandscape } from \"./effects/japanese-tower/JapaneseTowerLandscape\";\nimport \"./effects/japanese-tower/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><JapaneseTowerLandscape country=\"japan\" sourceUrl=\"/japanese-tower.html\" /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<JapaneseTowerLandscape country=\"china\" sourceUrl=\"/japanese-tower.html\" />\n```\n\n## Behavior contract\n\n- Runtime: Three.js r149 + Canvas 2D\n- Passes: 1 live Three.js scene render with six authored architecture builders and procedural CanvasTexture generation\n- Interaction: Country selection, automatic 4.4-second construction, pointer orbit, hover parallax, pinch and wheel zoom, and camera reset\n- Assets: 1 exact self-contained authored HTML scene with six country architectures, embedded Three.js runtime, procedural landscape, and texture set\n- **renderer** (sandbox): Three.js r149\n- **country** (variant): Japan / China / Vietnam / Thailand / Cambodia / Turkey\n- **structure** (fixed): Six authored procedural architectures\n- **landscape** (fixed): Terrain + mountains + grass + stones\n- **camera** (pointer): Orbit + parallax + zoom\n- **pixelRatio** (adaptive): ≤ 2\n- **assets** (embedded): Runtime + texture set\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "bookshelf": "---\nname: add-bookshelf\ndescription: \"Build Bookshelf from its verified authored source using Three.js r165, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Bookshelf\n\n## Description\n\nThe exact seven-volume Bookshelf collection with its authored room, carousel shelf, individual cover artwork, foil, pages, inspection, opening, and page-turn system.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React and TypeScript lifecycle host\n- Three.js r165 with OrbitControls, RoomEnvironment, RoundedBoxGeometry, and RectAreaLightUniformsLib\n- Seven exact book records and seven authored cover-atlas crops\n- Procedural cloth, foil, page stacks, flexible page geometry, PMREM room lighting, and carousel shelf motion\n- Pointer selection, cover/page dragging, shelf/detail state transitions, orbit, pan, keyboard, and reduced motion\n\n## Verified source material\n\n- `complete-shelf/index.html — exact seven volumes, seven cover crops, and complete renderer`\n- `src/shaders/bookshelf/bookshelfRenderer.js`\n- `src/shaders/bookshelf/BookshelfScene.tsx`\n\nSource revision: `6ef16625e670b0285bb689bdebffc1d728c6deb1`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Pin Three.js r165 and carry over all seven book records and cover crops together; do not reduce the collection to one volume.\n3. Transfer the complete room, shelf, lighting, embedded cover/wood atlases, book rig, cloth, foil, and page geometry.\n4. Keep shelf selection, continuous navigation, inspection transitions, cover dragging, page turns, orbit, pan, and keyboard focus as one state machine.\n5. Expose selection and shelf/detail state to the host through callbacks without moving scene ownership into React state.\n6. Retain responsive camera targets, context handling, reduced-motion behavior, render scheduling, and complete disposal.\n7. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: Keep both owned atlases embedded exactly as authored; no network asset lookup is required.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { BookshelfScene } from \"./effects/bookshelf/BookshelfScene\";\nimport \"./effects/bookshelf/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><BookshelfScene /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\nconst renderer = createBookshelfRenderer(host, canvas, {\n  onReady: () => setReady(true),\n  onSelectionChange: ({ index, total, title }) => setSelection({ index, total, title }),\n  onModeChange: (mode) => setMode(mode),\n});\nawait renderer.ready;\n    return () => renderer.dispose();\n```\n\n## Behavior contract\n\n- Runtime: Three.js r165\n- Passes: 1 live scene render + PMREM environment bake\n- Interaction: Shelf navigation, volume selection, click-to-inspect, cover drag, paginated leaf drag, orbit, pan, and reset\n- Assets: 2 exact embedded owned atlases — cover artwork and walnut texture\n- **renderer** (host): Three.js r165\n- **collection** (fixed): 7 authored volumes\n- **environment** (precompute): PMREM room\n- **interaction** (pointer): Inspect + cover + pages\n- **pixelRatio** (adaptive): ≤ 2\n- **assets** (embedded): 2 exact owned atlases\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "structure-flow": "---\nname: add-structure-flow\ndescription: \"Build Structure Flow from its verified authored source using Three.js r128–r160, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Structure Flow\n\n## Description\n\nThirteen authored Three.js field studies collected as one family, spanning particle domes, horizons, orbital systems, matrices, topology, fluid fields, embers, and vortexes.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React typed variant host\n- Thirteen authored Three.js r128-r160 field renderers\n- Point clouds, ShaderMaterials, topology scenes, fluid fields, embers, and post-process bloom\n- Lazy-loaded renderer-specific lifecycles and controls\n\n## Verified source material\n\n- `src/shaders/structure-flow/StructureFlowCollection.tsx`\n- `Axiom-Structure-Flow (2).html — Three.js background`\n- `src/shaders/structure-flow/structureFlowRenderer.ts`\n- `src/shaders/structure-flow/StructureFlowBackground.tsx`\n- `src/shaders/emerald-horizon/EmeraldHorizonBackground.tsx`\n- `src/shaders/orbital-sphere/OrbitalSphereBackground.tsx`\n- `src/shaders/dot-matrix/DotMatrixBackground.tsx`\n- `src/shaders/neuform-isolated/NeuformIsolatedEffects.tsx`\n- `src/shaders/neuform-isolated/NeuformCraftEffects.tsx`\n- `src/shaders/neuform-isolated/NeuformBatchEffects.tsx`\n\nSource revision: `SHA-256 40eb5bac81e3`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Use StructureFlowCollection as the family entry point and select the exact authored renderer with the variant prop.\n3. Keep Structure Flow, Emerald Horizon, Orbital Sphere, Dot Matrix, Expanse Field, Logic Core, Dimensional Field, Data Field, Topology Field, Nebula, Fluid Field, Ember Storm, and Flux Vortex as independent scenes rather than blending them into one renderer.\n4. Preserve the source-exact Three.js revision, geometry, shaders, camera, palette, motion, and pointer behavior for every variant.\n5. Expose each renderer's own controls at the variant boundary and lazy-load only the selected implementation.\n6. Retain every source renderer's resize, visibility, animation-frame, context, and disposal lifecycle.\n7. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { StructureFlowCollection } from \"./effects/structure-flow/StructureFlowCollection\";\nimport \"./effects/structure-flow/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><StructureFlowCollection variant=\"emerald-horizon\" /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<StructureFlowCollection variant=\"flux-vortex\" speed={1} density={1} />\n```\n\n## Behavior contract\n\n- Runtime: Three.js r128–r160\n- Passes: 1–2 Three.js scene, point-cloud, or ShaderMaterial passes\n- Interaction: Variant-specific pointer, motion, geometry, opacity, mask, and palette controls\n- Assets: No external assets\n- **renderer** (variant): Three.js r128–r160\n- **variants** (fixed): 13 field studies\n- **controls** (adaptive): Renderer-specific\n- **assets** (fixed): None\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "warp-field": "---\nname: add-warp-field\ndescription: \"Build Warp Field from its verified authored source using Three.js r128, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Warp Field\n\n## Description\n\nNexus’s focused hero warp: 400 emerald additive streaks and 40 luminous tiles streaming through an authored deep-space fog field.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React lifecycle host\n- Pinned Three.js r128\n- 400 colored additive line segments\n- 40 luminous plane tiles and exponential fog\n\n## Verified source material\n\n- `Nexus-Edge-Compute.html — hero warp scene`\n- `src/shaders/warp-field/warpFieldRenderer.ts`\n- `src/shaders/warp-field/WarpFieldBackground.tsx`\n\nSource revision: `SHA-256 bd7c486164d8`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Pin Three.js 0.128.0 and carry only the complete primary hero-warp scene.\n3. Retain the exact random distributions, four-color palette, line lengths, tile geometry, materials, camera, fog, and wrap bounds.\n4. Advance both line endpoints and all tiles by the same authored speed so the warp stays coherent.\n5. Expose speed, opacities, FOV, brightness, and a final-canvas hue/saturation grade with source-exact defaults.\n6. Dispose cloned tile materials, both geometries, the streak material, observers, frames, and renderer.\n7. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { WarpFieldBackground } from \"./effects/warp-field/WarpFieldBackground\";\nimport \"./effects/warp-field/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><WarpFieldBackground /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\npositions[index * 6 + 2] += speed;\npositions[index * 6 + 5] += speed;\nif (positions[index * 6 + 2] > 200) wrapStreak(index);\nrenderer.render(scene, camera);\n```\n\n## Behavior contract\n\n- Runtime: Three.js r128\n- Passes: 1 Three.js scene render\n- Interaction: Customizable speed, streaks, tiles, color, camera, and brightness\n- Assets: No external assets\n- **renderer** (host): Three.js r128\n- **streaks** (fixed): 400\n- **tiles** (fixed): 40\n- **assets** (fixed): None\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "engraved-certificate": "---\nname: add-engraved-certificate\ndescription: \"Build Engraved Certificate from its verified authored source using Canvas 2D + DOM/CSS, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Engraved Certificate\n\n## Description\n\nA responsive engraved certificate: plate field, dual guilloche rosettes, and a drifting harmonic pass that auto-cycles through four cam states.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React component with a sandboxed `srcDoc` effect boundary\n- Canvas 2D engraving passes and certificate DOM copied from the byte-exact Neuform export\n- A post-load focus adapter that keeps only the authored shader, button, canvas, or visual targets visible\n- Optional outer-frame hue, saturation, and brightness grading with source-exact defaults\n\n## Verified source material\n\n- `src/shaders/neuform-isolated/sources/kinetic-lathe-certificate.html`\n- `src/shaders/neuform-isolated/NeuformCraftEffects.tsx`\n\nSource revision: `SHA-256 dc48266f23df`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Copy the complete canonical HTML source byte-for-byte so its shader strings, materials, DOM, timing, and initialization order remain auditable.\n3. After the source load event, retain only the `#cert` certificate; do not rewrite the renderer or approximate the composition.\n4. Force retained background targets to the sandbox viewport and center retained buttons without changing their internal pointer or shader state.\n5. Dispatch one resize event after reparenting so the exact source renderer recalculates its backing resolution.\n6. Apply optional hue, saturation, and brightness only to the outer iframe; omit the filter at 0/1/1 so source color remains exact.\n7. Keep the sandbox isolated with `allow-scripts` only; removing the iframe must release its document, listeners, frames, and graphics contexts together.\n8. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { EngravedCertificate } from \"./effects/engraved-certificate/EngravedCertificate\";\nimport \"./effects/engraved-certificate/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><EngravedCertificate /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\nconst focusedSource = canonicalHtml\n  .replace(\"</head>\", focusStyles + \"</head>\")\n  .replace(\"</body>\", focusAfterLoadScript + \"</body>\");\n\nreturn <iframe title=\"Focused source effect\" srcDoc={focusedSource} sandbox=\"allow-scripts\" />;\n```\n\n## Behavior contract\n\n- Runtime: Canvas 2D + DOM/CSS\n- Passes: 3 Canvas 2D engraving passes + DOM certificate\n- Interaction: Auto-cycled cam configurations + optional final-frame palette\n- Assets: No owned binary assets\n- **source** (fixed): Exact Neuform HTML\n- **focus** (host): Effect-only sandbox\n- **palette** (optional): Final-frame grade\n- **assets** (fixed): No owned binary assets\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "woven-cloth": "---\nname: add-woven-cloth\ndescription: \"Build Woven Cloth from its verified authored source using Three.js r160, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Woven Cloth\n\n## Description\n\nA Three.js woven-cloth simulation with Woven Cloth typography printed into its procedural textile so every letter deforms with the fabric.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React component with a sandboxed `srcDoc` effect boundary\n- Three.js r160 woven-cloth scene with canvas-texture typography copied from the byte-exact Neuform export\n- A post-load focus adapter that keeps only the authored shader, button, canvas, or visual targets visible\n- Optional outer-frame hue, saturation, and brightness grading with source-exact defaults\n\n## Verified source material\n\n- `src/shaders/neuform-isolated/sources/lumina-weavers-cloth.html`\n- `src/shaders/neuform-isolated/NeuformCraftEffects.tsx`\n\nSource revision: `SHA-256 9bfd56ef7579`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Copy the complete canonical HTML source byte-for-byte so its shader strings, materials, DOM, timing, and initialization order remain auditable.\n3. After the source load event, retain only the fixed cloth viewport; do not rewrite the renderer or approximate the composition.\n4. Force retained background targets to the sandbox viewport and center retained buttons without changing their internal pointer or shader state.\n5. Dispatch one resize event after reparenting so the exact source renderer recalculates its backing resolution.\n6. Apply optional hue, saturation, and brightness only to the outer iframe; omit the filter at 0/1/1 so source color remains exact.\n7. Keep the sandbox isolated with `allow-scripts` only; removing the iframe must release its document, listeners, frames, and graphics contexts together.\n8. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { WovenCloth } from \"./effects/woven-cloth/WovenCloth\";\nimport \"./effects/woven-cloth/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><WovenCloth /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\nconst focusedSource = canonicalHtml\n  .replace(\"</head>\", focusStyles + \"</head>\")\n  .replace(\"</body>\", focusAfterLoadScript + \"</body>\");\n\nreturn <iframe title=\"Focused source effect\" srcDoc={focusedSource} sandbox=\"allow-scripts\" />;\n```\n\n## Behavior contract\n\n- Runtime: Three.js r160\n- Passes: 1 Three.js cloth scene pass\n- Interaction: Typography deforms with the authored textile motion + optional final-frame palette\n- Assets: No owned binary assets\n- **source** (fixed): Exact Neuform HTML\n- **focus** (host): Effect-only sandbox\n- **palette** (optional): Final-frame grade\n- **assets** (fixed): No owned binary assets\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "performance-gauges": "---\nname: add-performance-gauges\ndescription: \"Build Performance Gauges from its verified authored source using DOM + CSS, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Performance Gauges\n\n## Description\n\nFour layered CSS instruments — tachometer, speedometer, turbo boost, and EV power — each isolated to one full-bleed dial with polar tick geometry, scale bands, and a self-testing needle sweep.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React variant host over a sandboxed `srcDoc` effect boundary\n- DOM and layered CSS copied from the byte-exact source document\n- A post-load focus adapter that keeps only the selected gauge card visible\n- Optional outer-frame hue, saturation, and brightness grading with source-exact defaults\n\n## Verified source material\n\n- `src/shaders/neuform-isolated/sources/performance-gauges.html`\n- `src/shaders/neuform-isolated/NeuformIsolatedEffects.tsx`\n\nSource revision: `SHA-256 2dc25058fc85`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Copy the complete canonical HTML source byte-for-byte so its dial geometry, scale bands, and needle timing remain auditable.\n3. Select tachometer, speedometer, boost, or power with the variant prop; each variant isolates one gauge card by id.\n4. After the source load event, retain only that card and let the square fit rule size it against the shorter sandbox axis.\n5. Leave the authored self-test sequence — full-scale sweep, fall to rest, settle with overshoot, idle flutter — untouched.\n6. Apply optional hue, saturation, and brightness only to the outer iframe; omit the filter at 0/1/1 so source color remains exact.\n7. Keep the sandbox isolated with `allow-scripts` only; removing the iframe must release its document, listeners, and timers together.\n8. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { PerformanceGauges } from \"./effects/performance-gauges/PerformanceGauges\";\nimport \"./effects/performance-gauges/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><PerformanceGauges /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<PerformanceGauges variant=\"speedometer\" />\n```\n\n## Behavior contract\n\n- Runtime: DOM + CSS\n- Passes: 4 layered CSS gauge compositions, one per variant\n- Interaction: Self-test needle sweep, settle, and idle flutter with a counting readout + optional final-frame palette\n- Assets: No owned binary assets\n- **source** (fixed): Exact Neuform HTML\n- **focus** (host): Effect-only sandbox\n- **palette** (optional): Final-frame grade\n- **assets** (fixed): No owned binary assets\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "uplink-loader": "---\nname: add-uplink-loader\ndescription: \"Build Uplink Loader from its verified Community source with the complete renderer, interactions, controls, and required assets.\"\n---\n\n# Build Uplink Loader\n\nA cinematic secure-uplink loader with stepped progress, illuminated telemetry ticks, neon readouts, technical corner markers, mirrored side rails, scanlines, and procedural grain.\n\n## Verified source material\n\n- `src/shaders/uplink-loader/uplink-loader.html`\n- `src/shaders/uplink-loader/UplinkLoader.tsx`\n\nSource revision: `SHA-256 f73bb2963501`\n\n## Requirements\n\n- Preserve the complete DOM + CSS + JavaScript renderer and every published Community option.\n- Preserve its interaction contract: Autonomous stepped progress sequence with phase labels, completion hold, and reset glitch.\n- Preserve its assets: No owned binary assets.\n- Verify resize, mobile, reduced motion, visibility, teardown, and console health.\n",
  "koi-studies": "---\nname: add-koi-studies\ndescription: \"Build Koi Studies from its verified Community source with the complete renderer, interactions, controls, and required assets.\"\n---\n\n# Build Koi Studies\n\nA tactile stack of three Japanese koi studies with CSS 3D depth, pointer tilt, drag and keyboard navigation, pixel-mask reveals, and animated halftone imagery.\n\n## Verified source material\n\n- `public/synthralos-halftone.html — byte-exact complete source`\n- `src/shaders/koi-studies/KoiStudies.tsx`\n\nSource revision: `SHA-256 32cf6493414a`\n\n## Requirements\n\n- Preserve the complete DOM + CSS 3D + Canvas 2D + WebGL renderer and every published Community option.\n- Preserve its interaction contract: Drag, tap, or use the arrow keys to cycle cards, with pointer tilt, reveal trails, responsive layout, and reduced-motion behavior.\n- Preserve its assets: 3 embedded JPEGs + 3 embedded MP4 clips + 1 inline halftone mask; the optional authored CDN shader has a CSS fallback.\n- Verify resize, mobile, reduced motion, visibility, teardown, and console health.\n",
  "article-headings": "---\nname: add-article-headings\ndescription: \"Build Article Headings from its verified authored source using DOM/CSS + Canvas 2D, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Article Headings\n\n## Description\n\nThree expressive text treatments collected in one family: a chromatic intro, a particle wordmark, and an audio-reactive identity lockup.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- A lazy React and TypeScript host for five text-animation variants\n- Semantic DOM headings with requestAnimationFrame decoding\n- Canvas 2D neon, particle-mask, and audio-bar renderers\n- A chromatic DOM/CSS intro isolated from its authored source document\n- Synchronized dark/light surfaces, palette controls, and reduced-motion handling\n\n## Verified source material\n\n- `ascii-page-transition-v1.html — article headings and decode lifecycle`\n- `src/shaders/article-headings/TextAnimationCollection.tsx`\n- `src/shaders/article-headings/articleHeadingDecode.ts`\n- `src/shaders/article-headings/ArticleHeadings.tsx`\n- `src/shaders/neuform-isolated/sources/glassblown-neon.html`\n- `src/shaders/neuform-isolated/sources/creator-studio-intro.html`\n- `src/shaders/neuform-isolated/sources/epilude-footer.html`\n- `src/shaders/neuform-isolated/sources/audio-wordmark.html`\n- `src/shaders/neuform-isolated/NeuformCraftEffects.tsx`\n- `src/shaders/neuform-isolated/NeuformIsolatedEffects.tsx`\n- `src/shaders/fonts/fragment-mono.woff2`\n\nSource revision: `5a736cd3c1f6f19802f61ebb10e1701b9f7aa26e / SHA-256 e14795f24ea8 / 8d2cfccf1140 / 26f0d8d04494 / 1545c354af8d`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Expose article-headings, neon-sign, threeui-intro, particle-wordmark, and audio-wordmark through one discriminated variant prop.\n3. Preserve the article decoder as semantic headings, including its eased reveal budget, scramble window, cleanup, and reduced-motion behavior.\n4. Keep the Neon Typography source and its Canvas 2D tubing, electrode, flicker, and bloom renderer intact.\n5. Keep the intro, particle, and audio documents as sandboxed authored sources, adapting only presentation, theme, and ThreeUI copy.\n6. Lazy-load each renderer, forward only its compatible props, and preserve the family’s per-variant control sets and preview media.\n7. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: Copy the exact extracted Fragment Mono file for the article metadata and keep the four bundled owned HTML source documents available to the collection adapters.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { TextAnimationCollection } from \"./effects/article-headings/TextAnimationCollection\";\nimport \"./effects/article-headings/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><TextAnimationCollection variant=\"article-headings\" mode=\"dark\" duration={560} stagger={140} /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<TextAnimationCollection variant=\"audio-wordmark\" mode=\"dark\" brightness={1} />\n```\n\n## Behavior contract\n\n- Runtime: DOM/CSS + Canvas 2D\n- Passes: Variant-dependent DOM/CSS or one to two Canvas 2D passes\n- Interaction: Authored text motion with responsive presentation, reduced-motion handling, and synchronized light/dark mode\n- Assets: Exact embedded Fragment Mono font; all other sources and marks are bundled inline\n- **renderer** (variant): DOM/CSS or Canvas 2D\n- **variants** (fixed): Intro | Particle | Audio\n- **mode** (optional): dark | light\n- **motion** (adaptive): Reduced-motion aware\n- **assets** (bundled): Fragment Mono + inline source documents\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "animated-top-dock": "---\nname: add-animated-top-dock\ndescription: \"Build Animated Top Dock from its verified authored source using DOM + CSS, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Animated Top Dock\n\n## Description\n\nSable’s top navigation dock with its authored downward spring expansion, proximity field, glass layers, focus behavior, and active menu state.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- Semantic nav and button controls with authored inline SVG icons\n- Scoped glass CSS with backdrop filtering and active/focus states\n- Per-item spring integration driven by horizontal pointer proximity\n- ResizeObserver, fine-pointer capability checks, keyboard focus, and reduced motion\n- The exact embedded Fragment Mono font for dock labels\n\n## Verified source material\n\n- `ascii-page-transition-v1.html — exact top menu dock`\n- `src/shaders/animated-top-dock/topDockController.ts`\n- `src/shaders/animated-top-dock/AnimatedTopDock.tsx`\n- `src/shaders/fonts/fragment-mono.woff2`\n\nSource revision: `5a736cd3c1f6f19802f61ebb10e1701b9f7aa26e`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Build one logo control and the five authored SYSTEM, METHOD, WORK, ACCESS, and NOTES items with the source icon geometry.\n3. Measure each item at rest, then compute a smoothstep influence from pointer distance using the authored 122 px proximity radius.\n4. Integrate each item toward its target with spring 0.19 and damping 0.70, applying at most 17 px width, 16 px height, and 3.5 px downward growth.\n5. Mirror pointer proximity for keyboard focus, retain the selected-item paper state, and reset cleanly when focus or pointer leaves.\n6. Disable resizing motion on coarse pointers, narrow screens, and reduced-motion systems while keeping the nav fully usable.\n7. Re-measure on resize and remove all observers, listeners, media-query handlers, and animation frames on teardown.\n8. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: Copy the exact extracted Fragment Mono file for the dock labels; all SVG menu icons are inline and require no other assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { AnimatedTopDock } from \"./effects/animated-top-dock/AnimatedTopDock\";\nimport \"./effects/animated-top-dock/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><AnimatedTopDock proximity={122} spring={0.19} damping={0.7} /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\nconst influence = smoothstep(clamp(1 - distance / 122, 0, 1));\nstate.velocity += (influence - state.value) * 0.19;\nstate.velocity *= 0.70;\nstate.value += state.velocity;\nitem.style.transform = `translateY(${state.value * 3.5}px)`;\n```\n\n## Behavior contract\n\n- Runtime: DOM + CSS\n- Passes: 1 spring layout pass across 6 dock items\n- Interaction: Pointer proximity, keyboard focus, active selection, reduced motion, and mobile static mode\n- Assets: Exact embedded Fragment Mono font extracted from the authored source\n- **renderer** (host): DOM + CSS\n- **items** (fixed): 1 logo + 5 menu items\n- **proximity** (default): 122 px\n- **spring** (default): 0.19 / 0.70\n- **motion** (adaptive): Pointer + focus + reduced motion\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "sketchbook": "---\nname: add-sketchbook\ndescription: \"Build Sketchbook from its verified authored source using DOM + CSS 3D, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Sketchbook\n\n## Description\n\nThe exact Singapore paper sketchbook with nested-strip page curls, direct dragging, tilt, zoom, a movable magnifying glass, and its complete authored plate set.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- Semantic HTML, JavaScript, and CSS 3D transforms\n- Eighteen nested strips per turning leaf for the page curl\n- Pointer drag/tap, keyboard navigation, cursor tilt, zoom, draggable loupe, intro riffle, and reduced motion\n- Fourteen exact artworks and three exact local font files\n- Optional sandboxed `srcDoc` iframe for strict style and event isolation\n\n## Verified source material\n\n- `sketchbook/index.html — complete interaction source`\n- `sketchbook/* — 14 artworks and 3 owned font files`\n- `src/shaders/sketchbook/sketchbookDocument.js`\n- `src/shaders/sketchbook/Sketchbook.tsx`\n\nSource revision: `3938bc8def563f89ed587e52ac35ddc056c0a5f0`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Copy the complete authored document and all 17 local assets without renaming or recompressing them.\n3. Preserve the page/spread model and build every turning leaf from the exact 18-strip nested transform structure.\n4. Keep direct dragging, tap turns, arrows, keyboard controls, tilt, zoom, loupe movement, intro riffle, and coarse-pointer behavior together.\n5. Resolve image and font URLs from one configurable asset base before loading the document.\n6. Honor reduced motion, clean up listeners and animation frames, and verify every spread and asset path.\n7. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: Copy all 14 artworks and three fonts under the configured asset base URL.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { Sketchbook } from \"./effects/sketchbook/Sketchbook\";\nimport \"./effects/sketchbook/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><Sketchbook assetBaseUrl=\"/sketchbook/\" /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\nconst documentSource = createSketchbookDocument(\"/sketchbook/\");\nreturn (\n  <iframe\n    title=\"Interactive Singapore sketchbook\"\n    srcDoc={documentSource}\n    sandbox=\"allow-scripts\"\n  />\n);\n```\n\n## Behavior contract\n\n- Runtime: DOM + CSS 3D\n- Passes: 18 nested CSS 3D strips per turning leaf\n- Interaction: Drag or tap pages, arrows, keyboard, cursor tilt, zoom, and draggable loupe\n- Assets: 14 exact artworks + 3 exact local font files\n- **renderer** (host): DOM + CSS 3D\n- **pageCurl** (fixed): 18 nested strips\n- **interaction** (pointer): Drag + zoom + loupe\n- **motion** (adaptive): Reduced-motion aware\n- **assetBaseUrl** (string): /sketchbook/\n- **assets** (fixed): 17 exact local files\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "constellation-field": "---\nname: add-constellation-field\ndescription: \"Build Constellation Field from its verified authored source using Canvas 2D + Raw WebGL, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Constellation Field\n\n## Description\n\nA family of particle networks, gateways, interface lines, defense traces, and topographic fields gathered into one configurable collection.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React variant host\n- Seven Canvas 2D sources\n- One raw WebGL source\n- Dark/light mode surfaces\n- Lazy-loaded isolated source sandboxes\n\n## Verified source material\n\n- `src/shaders/constellation-field/ConstellationField.tsx`\n- `src/shaders/neuform-isolated/NeuformBatchEffects.tsx`\n- `src/shaders/neuform-isolated/sources/constellation-field.html`\n- `src/shaders/neuform-isolated/sources/particle-drift.html`\n- `src/shaders/neuform-isolated/sources/particle-network.html`\n- `src/shaders/neuform-isolated/sources/gateway-flow.html`\n- `src/shaders/neuform-isolated/sources/connectivity-graph.html`\n- `src/shaders/neuform-isolated/sources/interface-lines.html`\n- `src/shaders/neuform-isolated/sources/defense-lines.html`\n- `src/shaders/neuform-isolated/sources/topo-field.html`\n\nSource revision: `SHA-256 1920ad4fe34f`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Keep Constellation Field as the public entry point and select constellation-field, particle-drift, particle-network, gateway-flow, connectivity-graph, interface-lines, defense-lines, or topo-field with the variant prop.\n3. Retain each complete authored source and its own isolated renderer instead of blending the scenes into a shared canvas.\n4. Expose the shared mode, motion, geometry, opacity, and palette controls at the collection boundary.\n5. Mount only the selected source so inactive variants do not allocate a canvas, WebGL context, or animation loop.\n6. Preserve each renderer's source isolation, resizing, animation controls, and iframe lifecycle.\n7. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { ConstellationField } from \"./effects/constellation-field/ConstellationField\";\nimport \"./effects/constellation-field/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><ConstellationField /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<ConstellationField variant=\"topo-field\" speed={1.2} density={1.1} />\n```\n\n## Behavior contract\n\n- Runtime: Canvas 2D + Raw WebGL\n- Passes: 1 active isolated source pass\n- Interaction: Variant selection plus customizable mode, speed, size, stroke width, length, density, opacity, and palette\n- Assets: No owned binary assets\n- **source** (fixed): Exact Neuform HTML\n- **variants** (fixed): Constellation + Drift + Network + Gateway + Connectivity + Interface + Defense + Topo\n- **focus** (host): Effect-only sandbox\n- **mode** (optional): dark | light\n- **speed** (number): 1\n- **size** (number): 1\n- **strokeWidth** (number): 1\n- **length** (number): 1\n- **density** (number): 1\n- **opacity** (number): 1\n- **palette** (optional): Final-frame grade\n- **assets** (fixed): No owned binary assets\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "portal-field": "---\nname: add-portal-field\ndescription: \"Build Portal Field from its verified authored source using Three.js r134 + Raw WebGL + Canvas 2D, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Portal Field\n\n## Description\n\nFive ambient field backgrounds collected across Three.js, raw WebGL, and Canvas 2D renderers.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React variant host\n- Three.js ShaderMaterial\n- Raw WebGL fullscreen renderers\n- Canvas 2D particle and ember composites\n- Lazy-loaded variant sources\n\n## Verified source material\n\n- `src/shaders/portal-field/PortalFieldCollection.tsx`\n- `src/shaders/neuform-isolated/NeuformBatchEffects.tsx`\n- `src/shaders/neuform-isolated/sources/portal-field.html`\n- `src/shaders/neuform-isolated/sources/flow-field.html`\n- `src/shaders/neuform-isolated/NeuformIsolatedEffects.tsx`\n- `src/shaders/neuform-isolated/sources/strata-cloud.html`\n- `src/shaders/stream-convergence/StreamConvergenceBackground.tsx`\n- `src/shaders/stream-convergence/streamConvergenceShaders.ts`\n- `src/shaders/bell-field/BellFieldBackground.tsx`\n- `src/shaders/bell-field/bellFieldShaders.ts`\n\nSource revision: `SHA-256 f90e34f83d51`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Keep PortalFieldCollection as the public entry point and select portal-field, flow-field, cloud-field, bell-field, or stream-convergence with the variant prop.\n3. Retain each authored renderer and composition at its existing implementation boundary.\n4. Keep the common palette and motion controls at the collection boundary while allowing renderer-specific props through the typed variant union.\n5. Lazy-load and mount only the selected renderer so inactive WebGL contexts, canvases, timers, and animation loops are not allocated.\n6. Preserve every renderer's resize, visibility, interaction, animation-frame, and cleanup lifecycle.\n7. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { PortalFieldCollection } from \"./effects/portal-field/PortalFieldCollection\";\nimport \"./effects/portal-field/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><PortalFieldCollection /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<PortalFieldCollection variant=\"flow-field\" speed={1.1} density={1.2} hue={8} />\n```\n\n## Behavior contract\n\n- Runtime: Three.js r134 + Raw WebGL + Canvas 2D\n- Passes: 1 selected ambient field composition\n- Interaction: Variant selection plus customizable motion, geometry, opacity, and palette\n- Assets: No owned binary assets\n- **source** (fixed): Exact Neuform HTML\n- **focus** (host): Effect-only sandbox\n- **speed** (number): 1\n- **size** (number): 1\n- **length** (number): 1\n- **density** (number): 1\n- **opacity** (number): 1\n- **palette** (optional): Final-frame grade\n- **assets** (fixed): No owned binary assets\n- **variants** (fixed): Portal + Flow + Cloud + Bell + Stream Convergence\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "diagnostics-panel": "---\nname: add-diagnostics-panel\ndescription: \"Build Diagnostics Panel from its verified authored source using Canvas 2D, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Diagnostics Panel\n\n## Description\n\nThree diagnostic illustration variants — layered planes, node cubes, and a flowing mesh — each isolated without page chrome or copy.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React component with a sandboxed `srcDoc` effect boundary\n- Canvas 2D + DOM/CSS copied from the byte-exact Neuform export\n- A post-load focus adapter that keeps only the authored shader, button, canvas, or visual targets visible\n- Optional outer-frame hue, saturation, and brightness grading with source-exact defaults\n\n## Verified source material\n\n- `src/shaders/neuform-isolated/sources/diagnostics-panel.html`\n- `src/shaders/neuform-isolated/NeuformBatchEffects.tsx`\n\nSource revision: `SHA-256 6d05694bf006`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Copy the complete canonical HTML source byte-for-byte so its shader strings, materials, DOM, timing, and initialization order remain auditable.\n3. After the source load event, retain only the #main-container surface; do not rewrite the renderer or approximate the composition.\n4. Force retained background targets to the sandbox viewport and center retained buttons without changing their internal pointer or shader state.\n5. Dispatch one resize event after reparenting so the exact source renderer recalculates its backing resolution.\n6. Apply optional hue, saturation, and brightness only to the outer iframe; omit the filter at 0/1/1 so source color remains exact.\n7. Keep the sandbox isolated with `allow-scripts` only; removing the iframe must release its document, listeners, frames, and graphics contexts together.\n8. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { DiagnosticsPanel } from \"./effects/diagnostics-panel/DiagnosticsPanel\";\nimport \"./effects/diagnostics-panel/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><DiagnosticsPanel /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\nconst focusedSource = canonicalHtml\n  .replace(\"</head>\", focusStyles + \"</head>\")\n  .replace(\"</body>\", focusAfterLoadScript + \"</body>\");\n\nreturn <iframe title=\"Focused source effect\" srcDoc={focusedSource} sandbox=\"allow-scripts\" />;\n```\n\n## Behavior contract\n\n- Runtime: Canvas 2D\n- Passes: 1 active isolated Canvas 2D illustration\n- Interaction: Variant selection plus customizable speed, size, opacity, and palette\n- Assets: No owned binary assets\n- **source** (fixed): Exact Neuform HTML\n- **focus** (host): Effect-only sandbox\n- **mode** (optional): dark | light\n- **speed** (number): 1\n- **size** (number): 1\n- **length** (number): 1\n- **density** (number): 1\n- **opacity** (number): 1\n- **palette** (optional): Final-frame grade\n- **assets** (fixed): No owned binary assets\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "skeuomorphic-toggle": "---\nname: add-skeuomorphic-toggle\ndescription: \"Build Skeuomorphic Toggle from its verified authored source using DOM/CSS, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Skeuomorphic Toggle\n\n## Description\n\nA tactile skeuomorphic toggle with a sliding on/off thumb that automatically matches light and dark appearances, isolated without the surrounding card.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React component with a sandboxed `srcDoc` effect boundary\n- DOM/CSS copied from the byte-exact Neuform export\n- A post-load focus adapter that keeps only the authored shader, button, canvas, or visual targets visible\n- Optional outer-frame hue, saturation, and brightness grading with source-exact defaults\n\n## Verified source material\n\n- `src/shaders/neuform-isolated/sources/skeuomorphic-toggle.html`\n- `src/shaders/neuform-isolated/NeuformBatchEffects.tsx`\n\nSource revision: `SHA-256 3e19e7fec9ac`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Copy the complete canonical HTML source byte-for-byte so its shader strings, materials, DOM, timing, and initialization order remain auditable.\n3. After the source load event, retain only the #skeuomorphic-toggle control; do not rewrite the renderer or approximate the composition.\n4. Force retained background targets to the sandbox viewport and center retained buttons without changing their internal pointer or shader state.\n5. Dispatch one resize event after reparenting so the exact source renderer recalculates its backing resolution.\n6. Apply optional hue, saturation, and brightness only to the outer iframe; omit the filter at 0/1/1 so source color remains exact.\n7. Keep the sandbox isolated with `allow-scripts` only; removing the iframe must release its document, listeners, frames, and graphics contexts together.\n8. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { SkeuomorphicToggle } from \"./effects/skeuomorphic-toggle/SkeuomorphicToggle\";\nimport \"./effects/skeuomorphic-toggle/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><SkeuomorphicToggle /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\nconst focusedSource = canonicalHtml\n  .replace(\"</head>\", focusStyles + \"</head>\")\n  .replace(\"</body>\", focusAfterLoadScript + \"</body>\");\n\nreturn <iframe title=\"Focused source effect\" srcDoc={focusedSource} sandbox=\"allow-scripts\" />;\n```\n\n## Behavior contract\n\n- Runtime: DOM/CSS\n- Passes: 1 isolated source pass\n- Interaction: Automatic site/system appearance with explicit light and dark overrides, plus customizable size, opacity, and palette\n- Assets: No owned binary assets\n- **source** (fixed): Exact Neuform HTML\n- **focus** (host): Effect-only sandbox\n- **mode** (optional): auto | dark | light\n- **speed** (number): 1\n- **size** (number): 1\n- **length** (number): 1\n- **density** (number): 1\n- **opacity** (number): 1\n- **palette** (optional): Final-frame grade\n- **assets** (fixed): No owned binary assets\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "matrix-field": "---\nname: add-matrix-field\ndescription: \"Build Laser from its verified authored source using Raw WebGL, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Laser\n\n## Description\n\nFour pointer-reactive laser scenes spanning a preserved matrix junction, atmospheric blade, vanishing array, and halftone relay.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React component with a sandboxed `srcDoc` effect boundary\n- Raw WebGL copied from the byte-exact Neuform export\n- A post-load focus adapter that keeps only the authored shader, button, canvas, or visual targets visible\n- Optional outer-frame hue, saturation, and brightness grading with source-exact defaults\n\n## Verified source material\n\n- `src/shaders/laser/LaserCollection.tsx`\n- `src/shaders/laser/LaserVariants.tsx`\n- `src/shaders/laser/laserShaders.ts`\n- `src/shaders/neuform-isolated/NeuformBatchEffects.tsx`\n- `src/shaders/neuform-isolated/sources/matrix-field.html`\n\nSource revision: `SHA-256 70cc015d5175`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Copy the complete canonical HTML source byte-for-byte so its shader strings, materials, DOM, timing, and initialization order remain auditable.\n3. After the source load event, retain only the #glcanvas surface; do not rewrite the renderer or approximate the composition.\n4. Force retained background targets to the sandbox viewport and center retained buttons without changing their internal pointer or shader state.\n5. Dispatch one resize event after reparenting so the exact source renderer recalculates its backing resolution.\n6. Apply optional hue, saturation, and brightness only to the outer iframe; omit the filter at 0/1/1 so source color remains exact.\n7. Keep the sandbox isolated with `allow-scripts` only; removing the iframe must release its document, listeners, frames, and graphics contexts together.\n8. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { LaserCollection } from \"./effects/matrix-field/LaserCollection\";\nimport \"./effects/matrix-field/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><LaserCollection /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\nconst focusedSource = canonicalHtml\n  .replace(\"</head>\", focusStyles + \"</head>\")\n  .replace(\"</body>\", focusAfterLoadScript + \"</body>\");\n\nreturn <iframe title=\"Focused source effect\" srcDoc={focusedSource} sandbox=\"allow-scripts\" />;\n```\n\n## Behavior contract\n\n- Runtime: Raw WebGL\n- Passes: 1 selected raw-WebGL laser pass\n- Interaction: Variant selection, smoothed pointer response, reduced-motion stills, and customizable motion, geometry, opacity, and palette\n- Assets: No external assets\n- **renderer** (host): Raw WebGL + preserved sandboxed WebGL\n- **variants** (fixed): Matrix Junction + Atmospheric Blade + Vanishing Array + Halftone Relay\n- **interaction** (pointer): Smoothed horizontal and vertical response\n- **pixelRatio** (adaptive): Capped at 1.5\n- **motion** (adaptive): Visibility-aware + reduced-motion still\n- **assets** (fixed): No external assets\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "wireframe-forms": "---\nname: add-wireframe-forms\ndescription: \"Build Wireframe Forms from its verified authored source using Canvas 2D, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Wireframe Forms\n\n## Description\n\nA family of rotating wireframe forms, with the cube, crossed cylinders, and nested sphere isolated as individual variants.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React component with a sandboxed `srcDoc` effect boundary\n- Canvas 2D copied from the byte-exact Neuform export\n- A post-load focus adapter that keeps only the authored shader, button, canvas, or visual targets visible\n- Optional outer-frame hue, saturation, and brightness grading with source-exact defaults\n\n## Verified source material\n\n- `src/shaders/neuform-isolated/sources/wireframe-forms.html`\n- `src/shaders/neuform-isolated/NeuformBatchEffects.tsx`\n\nSource revision: `SHA-256 828c6a635b54`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Copy the complete canonical HTML source byte-for-byte so its shader strings, materials, DOM, timing, and initialization order remain auditable.\n3. After the source load event, retain only the three wireframe canvases; do not rewrite the renderer or approximate the composition.\n4. Force retained background targets to the sandbox viewport and center retained buttons without changing their internal pointer or shader state.\n5. Dispatch one resize event after reparenting so the exact source renderer recalculates its backing resolution.\n6. Apply optional hue, saturation, and brightness only to the outer iframe; omit the filter at 0/1/1 so source color remains exact.\n7. Keep the sandbox isolated with `allow-scripts` only; removing the iframe must release its document, listeners, frames, and graphics contexts together.\n8. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { WireframeForms } from \"./effects/wireframe-forms/WireframeForms\";\nimport \"./effects/wireframe-forms/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><WireframeForms /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\nconst focusedSource = canonicalHtml\n  .replace(\"</head>\", focusStyles + \"</head>\")\n  .replace(\"</body>\", focusAfterLoadScript + \"</body>\");\n\nreturn <iframe title=\"Focused source effect\" srcDoc={focusedSource} sandbox=\"allow-scripts\" />;\n```\n\n## Behavior contract\n\n- Runtime: Canvas 2D\n- Passes: 1 isolated source pass\n- Interaction: Three selectable shape variants with customizable speed, size, length, density, opacity, and palette\n- Assets: No owned binary assets\n- **source** (fixed): Exact Neuform HTML\n- **focus** (host): Effect-only sandbox\n- **mode** (optional): dark | light\n- **speed** (number): 1\n- **size** (number): 1\n- **length** (number): 1\n- **density** (number): 1\n- **opacity** (number): 1\n- **palette** (optional): Final-frame grade\n- **assets** (fixed): No owned binary assets\n- **variant** (choice): Cube | Cylinders | Sphere\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n",
  "brand-orbs": "---\nname: add-brand-orbs\ndescription: \"Build Brand Orbs from its verified authored source using Canvas 2D, including the complete renderer, interactions, and required assets. Use when Codex needs to implement, port, or adapt this effect without requiring the ThreeUI package or reconstructing the visual from an approximation.\"\n---\n\n# Build Brand Orbs\n\n## Description\n\nTwenty-three animated brand marks rebuilt as small and medium dimensional dot orbs for AI status, product activity, and compact loading states.\n\nRecreate the authored behavior from the verified source, not from screenshots or the abbreviated orchestration sample in this skill. The implementation may live directly in the target project and does not require `@designcodeio/threeui`.\n\n## Technologies\n\n- React and TypeScript sandbox host\n- Exact authored Canvas 2D dot engine\n- Twenty-three embedded brand-mark variants\n- Small 20px and medium 56px presets\n- Dark-first theme\n- Reduced-motion and visibility-aware animation lifecycle\n\n## Verified source material\n\n- `brand-orbs-v2.html — complete authored specimen library`\n- `src/shaders/brand-orbs/sources/brand-orbs-v2.html`\n- `src/shaders/brand-orbs/BrandOrbs.tsx`\n\nSource revision: `SHA-256 c2733de8cf7b`\n\n## Implementation steps\n\n1. Open every verified source file listed above and identify the renderer, host lifecycle, styles, and assets before editing.\n2. Keep the complete Brand Orbs V2 document byte-for-byte as the canonical provenance source.\n3. Use BrandOrbs as the public entry point and select claude, openai, codex, cursor, gemini, figma, framer, react, swift, designcode, aura, dreamcut, ui, ux, css, ios, neuform, github, x, instagram, threads, linkedin, or email with the variant prop.\n4. Extract only the authored orb engine into an allow-scripts-only srcDoc and mount one canvas so the separate book and study specimens do not allocate render loops.\n5. Use size small for compact inline status and medium for the default presentation; do not stretch either tuned preset with CSS transforms.\n6. Keep the default dark mode for the catalog presentation, switch to light only for a known light surface, and use paused to freeze the current frame rather than overloading speed.\n7. Pause time and drawing when the host is offscreen, the document is hidden, or paused is true; preserve the authored reduced-motion still frame.\n8. Give the local component a sized, overflow-controlled parent and verify desktop, mobile, reduced-motion, and context-loss behavior.\n\nAsset handling: This effect has no required external assets.\n\n## Local component example\n\nImport the copied local component rather than a package entrypoint:\n\n```tsx\nimport { BrandOrbs } from \"./effects/brand-orbs/BrandOrbs\";\nimport \"./effects/brand-orbs/styles.css\";\n\nexport function Scene() {\n  return <div className=\"effect-frame\"><BrandOrbs /></div>;\n}\n```\n\n## Core renderer pattern\n\nThis excerpt documents orchestration only. Copy the exact shader, geometry, pass, and interaction code from the verified source files.\n\n```tsx\n<BrandOrbs variant=\"openai\" size=\"medium\" mode=\"dark\" speed={1} aria-label=\"OpenAI is reasoning\" />\n```\n\n## Behavior contract\n\n- Runtime: Canvas 2D\n- Passes: 1 transparent Canvas 2D dot-lattice pass\n- Interaction: Twenty-three variants, small and medium presets, dark-first theme, speed, pause, reduced motion, visibility pause, and deterministic restart\n- Assets: Embedded vector paths and procedural geometry; no external runtime assets\n- **renderer** (host): Sandboxed authored Canvas 2D engine\n- **variants** (fixed): 23 brand marks\n- **size** (fixed): Small 20px | Medium 56px (default)\n- **theme** (adaptive): Dark (default) | Light\n- **motion** (adaptive): Speed | Pause | Reduced motion | Visibility\n- **assets** (embedded): Vector paths + procedural dots\n\n## Verification\n\n1. Compare the rendered composition, animation timing, pointer behavior, and state transitions with the source implementation.\n2. Exercise resize, high-DPI, mobile/coarse-pointer, reduced-motion, tab visibility, and WebGL context-loss paths where applicable.\n3. Confirm every animation frame, observer, listener, geometry, buffer, texture, framebuffer, material, and renderer is released on teardown.\n4. Check the browser console and confirm the effect renders at native-or-better backing resolution.\n\n## Guardrails\n\n- Do not substitute a visually similar package, demo, shader, or runtime.\n- Do not approximate, reconstruct, or simplify the authored GLSL, render passes, geometry, interaction state, or assets.\n- Keep exact source and asset hashes under regression tests when the source project provides them.\n- Adapt only the surrounding host boundary needed by the target project; keep renderer behavior intact.\n"
};
export function buildSkillMarkdown(shader) { const skill = SKILLS[shader.id]; if (!skill) throw new Error(`No standalone implementation guide exists for ${shader.id}.`); return skill; }
